# 成长回路（Growth Loop）

一个把“今天做了什么”变成下一步行动的自我提升 Agent。用户可以直接和 AI 对话，记录学习、运动、生活和休息；Agent 负责整理、安排、复盘，并把可验证的行动记入成长轨迹。

> 当前版本是可运行的原型，可部署为邀请制公网 Demo，但不是生产 SaaS。项目已经包含 Next.js Web、Capacitor Android 工程、确定性 demo 数据、OpenAI-compatible LLM 接口、微信公众号明文回调、理解测验和电脑 Android Emulator 调试链路。

## 变更记录

### 2026-09-23 · 题干完整性与代码阅读排版

- 修复课后题 prompt 在规范化时硬截到 800 字符的问题，题干/提示/参考答案/rubric 保留换行；每字段上限 12000 字符，超限或围栏未闭合时执行已有的一次结构修复，失败则拒绝出题，不用截断内容伪装成功。
- 出题约束使用短小完整案例，背景/材料/问题分段，代码及轨迹使用带语言的代码围栏。课堂两处答题列表统一安全文本渲染：保留段落/换行/缩进，支持围栏和旧 role 轨迹代码块、行内代码；提示折叠、代码区可键盘聚焦并横向滚动，题目仍为原 12px，未修改全局字号。不执行生成 HTML。
- 新增长代码不截断、超长/未闭合代码拒绝、静态 React 渲染和 HTML 转义测试。历史已被截断的题目未覆盖，重新出题需要新的题目版本，不能凭排版找回缺失尾部；旧考试记录不变。

### 2026-09-23 · 课程与今日任务自动联动

- 课程保存、课节生成和考核通过事务同步任务；只为 available/passed 课节建立 learn 任务及 task_lesson_links，携带正确 goal_id。重复保存、生成或同步复用任务；锁定课节不提前创建任务。课程版本归档时隐藏旧的未完成任务，保留完成历史。
- 原有手动“加入今日”接口本来可以建立绑定，缺的是自动调用链路；纠正前次日志中“没有写入路径”的不准确表述。手动加入会复用自动任务，课程任务的手动完成接口仍返回 409。
- 旧记录按账号补建：`node --env-file=.env.local scripts/sync-course-tasks.mjs USERNAME`。本次仅执行 `test`，未修改其他账号、成绩或课程内容；补建已通过课节任务的时间取课节历史更新时间。
- 真实回归：第三课测试答案（仅依据公开课堂）获 88/良，第三课任务由 current→done，第四课任务自动创建为 current，目标进度 50%，能力掌握度 88/证据 1 条。第四课未答题，手动完成实测 409。第三课长题存在题干截断，测试答案明确借助公开示例补足；这是独立内容缺陷，尚待修复，不能将本次功能分数当成教学质量或真人学习效果数据。
- 回归增加自动任务、未通过不完成、不可手动完成、重试幂等、越权拒绝与归档历史保留检查；前端服务已加载此次构建。

### 2026-09-23 · 第二课至第三课真实闭环测试

- 使用 `test` 账号，通过公开业务接口读取第二课题目、提交预先编写的测试答案（未读取评分参考答案）、评分并流式生成第三课。第二课得分 75/良、passed；能力掌握度 75、confidence 0.57、独立证据 1 条；目标进度由 17% 更新为 33%。
- 第三课从 locked 解锁为 available，资料审核通过，课程 generation=ready、quality=passed（88 分），3 道题已保存；第四课仍 locked。测试为接口级业务验收，不代表已完成浏览器交互或整门课程最终考核。
- **未通过的环节：今日任务联动**。该目标没有 tasks，课程也没有 task_lesson_links；评分代码只更新已有绑定任务，因此这次没有任务被完成。当前运行代码搜索未发现创建课程任务绑定的写入路径。学习→考核→画像/进度→下一课已跑通，但完整“今日任务—课程”闭环仍需补齐任务创建/绑定，不应再表述为任务联动已验收。

### 2026-09-23 · 原始课节目标约束与逐项证据审核

- 课节生成将数据库中的原始标题/目标作为可信审核范围，改写后的 query 和 targetTopics 只负责检索，不再提升教学要求（例如把“识别异常”升级成“识别并实施避免措施”）。
- 门禁决策按原始 action、课节目标、资料模式及策略版本生成指纹；旧记录无指纹或目标改变时重新决策，并清除旧审核缓存，不删除历史检索与成绩。
- 逐项审核服务端从原目标切出的要求，保存 covered/partial/missing、引用和支持说明；校验要求齐全、引用 ID 归属和逐字原文，只有全部覆盖且引用有效才能通过。时效问题仍需独立版本依据。送审仅保留片段 ID、来源名和原文，去除 excerpt/snapshotText 重复传输。
- 此次仍在既有 TypeScript 审核节点内修复契约，不新增 Agent 或跨步骤编排；迁移 Pydantic AI 时须保留相同输入边界与校验。
- 真实验收：重启 3011 后，`test` 原第二课通过两项原始要求的引用核验，正文与 3 道题成功保存，generation=ready、quality=passed（80 分）、source=grounded。仍有非阻断教学改进建议，不宣称整门课程或后续评分已验收。本次审核输入 4791 token；此前为 12220，输入内容并不完全相同，仅记录本次运行，不作为通用节省率。
- 回归：19/19 学习测试套件、typecheck、lint、生产构建通过，新增目标变更缓存失效、缺少逐项检查、伪造原文引用及部分覆盖拒绝放行测试。

### 2026-09-23 · 第二课检索遗漏修复（连续生成仍待验收）

- Knowledge Gate 的 `targetTopics` 传入现有 RAG Tool；RAGFlow 每次逻辑检索最多三个同权限请求（原问题及两组细化知识点），结果去重后交错排列，防止概述占满证据包。保持 2400 token 预算，不增加联网轮次、不绕过指定教材限制；检索记录保存 targetTopics。
- 证据审核不再接收整门课程标题/描述，只读取当前查询和证据；明确禁止扩展后续章节要求、把“必要性”改写为所有任务都必须迭代的普遍命题。更新门禁缓存指纹，避免沿用旧策略结果。
- 真实教材回归已找回停止条件、循环失控、工具结果丢失导致重复调用等片段。但第二课仍被审核判为三类异常的识别/避免覆盖不足，**未宣称第二课生成通过**；需要继续验证证据审核的逐项支持关系。未更改成绩、教材或课程目标，未关闭门禁。
- 增加小结果预算下各查询均保留候选的回归测试，以及审核输入不携带整门课程目标的测试。RAGFlow 权限、不可变引用和上游故障测试继续保留。
- 验证：19/19 学习回归套件、typecheck、lint、生产 build 通过。真实检索与证据审核直接执行新代码验证；已有长期运行的生产进程未重启，不代表浏览器中的旧进程已切换新构建。

### 2026-09-22 · RAGFlow 真实首课闭环验收与证据范围修复

- `test` 账号同一目标完成真实规划、RAGFlow 检索、首课生成、课堂答疑、两次评分和下一课解锁：首课 grounded、质量 84 分、3 道题；不足答案 0 分不解锁，预先撰写的完整测试答案 95 分后首课 passed、下一课 available、目标进度 17%。这是一次功能验收，不是教学效果或准确率评测。
- 修复单一教材仍被“每来源最多一半片段”规则截断；RAGFlow 课程默认最多 20 个短片段，保持原 2400 token 证据预算。多问句最多拆成 3 个同权限检索子请求并做去重排名融合，单问题不扩展；不突破知识门禁补充轮次。
- 证据审核只考察当前 query，总目标不再隐式成为每节课的覆盖清单；允许依据片段事实进行直接对照/归纳，不要求教材逐字提供成品教案，仍禁止用模型记忆补缺失事实。
- **未完成**：第二课已解锁但生成停在指定教材证据不足；候选未充分覆盖停止条件、无限循环/过度调用/忽略观察结果的识别与处理。未关闭严格指定资料规则，未宣称整个连续学习闭环通过。上传/联网入库迁移仍待完成。
- 测试采用公开业务接口，没有直接修改成绩或阅读数据库答案出题；同题重答不作为新独立掌握度证据，因此首轮 0 分后的掌握度不会被重复作答直接抬高。测试目标与记录保留在 `test` 账号，修复版本本次运行在 3011 端口。

### 2026-09-22 · 真实闭环验收进行中：服务故障可恢复

- 新增 opt-in `scripts/ragflow-learning-live.mjs`，使用 `test` 账号执行目标创建、准备流、课堂提问、人工测试答案评分和下一课生成；只通过公开业务接口，不读取参考答案冒充学习验证。
- 本次真实运行已创建目标「Agent 工具调用与 ReAct 循环（闭环验收）」并经过能力/课程规划阶段；workflow 服务未运行已启动，随后发现 Docker 引擎未运行且 RAGFlow 8088 端口拒绝连接。课程、答疑、评分和解锁尚未完成，不能声明闭环通过。
- RAGFlow 连接拒绝、超时、HTTP/业务错误返回安全的明确提示；知识门禁将检索服务故障记为 `service_unavailable`，不缓存成长期资料不足，不因服务故障自动联网或继续伪装资料教学。恢复服务后可在原目标重试，无需改资料范围。
- 补充服务恢复回归；真实测试流使用增量 UTF-8 解码，错误/提前断流以非零退出，不再把 HTTP 200 内的错误事件当作测试通过。

### 2026-09-22 · RAGFlow 检索接入（已建资料库）

- 保留 RAG Tool 与 LangGraph/Pydantic AI 边界；已绑定文档的课程生成、课堂答疑检索直连 RAGFlow retrieval API，由知识库自身配置的 BGE-M3 编码查询，不再调用本地 LlamaIndex 排序或重新向量化。
- 新增本机管理脚本 `scripts/ragflow-import.mjs`：显式指定学习助手账号、知识库与文档，将 RAGFlow 原始片段保存为可查看、可引用的业务快照；不是重新切片。相同绑定重复运行复用已有记录。
- 账号权限、目标选中/排除范围、引用 ID、证据审核、检索计时及不可变快照继续保留。空范围不调用远端；返回越权片段、业务错误、正文变更或服务离线时明确失败，不默默改用旧片段。
- RAGFlow 资料禁止在本项目重新切片/向量化；查看器明确显示远端片段快照，不把它伪装成自建父子块或完整向量化输入。Docker 继续由用户手动启动，未改用户启动脚本。
- 范围：本轮接入现有 RAGFlow 文档；上传和联网收录仍走旧链路，混合范围中旧资料使用 FTS5 补充并记录降级原因。未迁移 RAGFlow Chat/Agent，也未验证完整真实教学评分闭环。
- 验证：typecheck、lint、build；19/19 隔离回归通过。真实 RAGFlow 在独立测试数据库导入 2644 个片段，并通过实际 RAG Tool 返回 3 个相关片段（本次约 767 ms，非性能基准）。实际账号绑定需用户明确指定用户名。
- 操作说明见 [RAGFlow 部署与接入](docs/RAGFLOW_DEPLOYMENT.md#7-已建知识库接入2026-09-22)。

### 2026-09-16 · RAGFlow 独立本机部署配置

- **新增** `deploy/ragflow/`：固定 RAGFlow v0.27.2 镜像及 digest，复用已有 ES 8.17.3 镜像，但所有服务使用独立 `growth-loop-ragflow` 容器/网络/数据卷，不碰旧 ES 或学习助手资料。
- **安全与资源**：只开放 `127.0.0.1:8088`，依赖端口不对外开放；首次初始化随机生成私有密码并排除 Git；单 task executor、任务/分块/编码并发 1，关闭 datasync/Go/Admin/MCP/沙箱，设置容器内存上限、日志轮转，不随 Docker 自动重启。
- **启停入口**：`start-ragflow.cmd` / `stop-ragflow.cmd`；支持 Init、Check、Status、Logs。停止保留数据，不提供清卷操作；丢失凭据且已有数据卷时拒绝重新生成密码。
- **迁移边界**：按用户决定将目标 RAG 底层改为 RAGFlow，保留 RAG Tool、权限与证据快照，LangGraph/Pydantic AI 职责不变；旧 LlamaIndex 路径尚未切换。MinerU/Qwen 为后续独立接入，未重复下载模型或修改业务 `.env.local`。
- **验证**：配置初始化、Compose 静态校验、Git 忽略、重复初始化不覆盖密码、回环端口隔离、PowerShell 语法检查通过。5 个服务启动成功，页面 HTTP 200，版本 v0.27.2，数据库/ES/对象存储/队列健康均为 ok；停止后重启通过，原有容器与本次 6 个数据卷均保留。初次空闲容器内存合计约 4.6GiB（单次快照，不含宿主/WSL 开销，不代表解析峰值）。未完成模型/文件/检索效果测试。
- 使用说明：[RAGFlow 部署与后续接入](docs/RAGFLOW_DEPLOYMENT.md)。

### 2026-09-15 · 进程归属记录与 RAG 回归修复

- **修复启动记录**：Windows `Start-Process` 返回后 `Process.Path` 偶尔为空。现在保存启动前解析、验证过的实际可执行文件路径，不再把空值写入归属记录；停止时仍核对实时路径和启动时间，不放宽进程身份校验。
- **停止兼容**：实时路径暂不可读时使用 CIM 查询；Python 启动器随子进程自动退出的竞态不再误报失败，仅在确认进程已退出或 PID 已更换时忽略异常，真正的存活进程拒绝访问仍报错。已退出进程的旧空路径记录可以安全跳过，存活进程的损坏记录仍拒绝猜测认领。
- **更新旧回归**：`v044-rag-tool-smoke.mjs` 使用隔离数据库和模拟模型接口，明确覆盖审核通过、证据不足、服务不可用、未配置模型四个分支，验证后三种情况不得提供已审核教学证据；保留权限、范围、预算、来源快照和恢复记录检查。未修改生产教学审核规则。
- **回归入口**：旧 RAG 测试纳入 `npm run verify:learning`；18/18 组通过，lint、typecheck 通过。模型接口为模拟，不调用真实 LLM。
- **实测启停**：三个本地服务启动成功，记录路径均有效，`-CheckOnly`、统一停止和重复停止均通过；6000/8010/8020 已释放。测试服务已关闭，未处理或修改用户文件、课程及业务资料库。

### 2026-09-15 · RAG 配套停止脚本

- 新增根目录 `stop-rag.cmd`：双击停止启动脚本管理的 RAG、Embedding、MinerU；`stop-rag.cmd -Service RAG` 只停止 RAG，`stop-rag.cmd -CheckOnly` 只查看。
- 启动脚本新增进程归属记录（PID、启动时间、可执行路径、项目目录），在健康检查前保存；停止脚本核对归属并处理记录进程的子进程，不按端口或 Python 名称批量关闭，不删除日志和资料，也不针对 Next.js/workflow。
- 停止属于进程终止操作，正在解析或编码的任务会被打断，建议先等当前任务结束。本轮没有实际停止用户服务。
- **旧进程注意**：升级前启动或手动启动且没有记录的服务不会被自动认领；需要在原终端手动关闭一次，再用新版启动脚本启动。启动脚本复用外部服务时也不会将其归为自己管理。

### 2026-09-15 · LlamaIndex 编码与向量检索接入

- **已接入**：RAG Python 服务新增 `/v1/embeddings` 与 `/v1/retrieve/vectors`。LlamaIndex `BaseEmbedding` 适配原有本地 GPU 服务；Next.js 的文档和查询编码统一经过该入口，默认模型仍由 `EMBEDDING_MODEL` 控制，不自动下载云端模型。
- **检索**：目标范围和单资料向量检索都交给 LlamaIndex `VectorStoreIndex`/Retriever；业务服务先按权限、活动片段及模型 profile 筛选，再分批发送向量并验证返回 ID。SQLite 仍保存向量，初版每批最多 128 个候选建立临时内存索引后合并精确排序，不是新建持久化向量库，也不宣称性能提升。原有 FTS5/auto 降级、Evidence Review 和证据预算保留。
- **切片查看保留**：现有父子片段、搜索与来源信息不变，子片段下新增“查看向量化输入”，显示 trim 后上下文前缀与正文的实际拼接。MinerU 和切片参数未改变。
- **运行要求**：重启 RAG Python 服务和 Next.js；GPU/MinerU 无需因本次改动重新下载或重启。`start-rag-stack.ps1` 导入 GPU 服务地址与编码超时；已在运行的旧 RAG 进程不会被脚本自动替换，需先停止旧进程再启动。所有本地服务只应监听回环地址，不将 RAG/GPU 端口暴露到公网。
- **数据与验收边界**：未删除或重算用户资料。按用户要求，不运行真实文件、模型效果或向量对照测试；用户自行上传文件验收。框架接入不等于整个 RAG 已迁移成独立持久化服务，剩余存储/关键词适配边界见迁移文档。
- **代码检查**：lint、typecheck、Python 模块导入和 API schema 生成通过；未运行真实检索、模型调用或文件处理效果测试。

### 2026-09-15 · RAG 内部迁移实施方案

- 新增 [LlamaIndex 迁移方案](docs/RAG_LLAMA_INDEX_MIGRATION.md)，固定 RAG Tool 契约、既有 SQLite 向量复用、Embedding 一致性、资料权限、分阶段切换及验收用例。
- 明确不清库、不同时修改切片参数、不新增教学生成器；框架迁移不能绕过 Evidence Review，也不能丢失历史来源 ID。
- 当前完成范围与代码入口核查，尚未切换运行后端；M0–M4 实施进度在文档末尾跟踪。

### 2026-09-15 · 固定框架职责与后续开发约束

- **架构确认**：跨步骤的状态、分支和恢复统一交给 LangGraph；节点内部的模型调用、工具执行和结构化输出交给 Pydantic AI。工具权限、业务事务及幂等仍由业务服务保证。
- **RAG 方向**：保留 RAG Tool 契约，内部实现逐步交给 LlamaIndex；MinerU 继续负责解析。不得再引入另一套教学回答或跨步骤编排流程。
- **文档与项目约定**：新增 [框架职责与编排边界](docs/ARCHITECTURE_BOUNDARIES.md)，同步角色设计和根目录 `AGENTS.md`；`CLAUDE.md` 已通过 `@AGENTS.md` 引用同一约定。
- **实现状态**：本次仅修改文档。Tutor/证据审核的 TypeScript 结构化调用、现有自写向量检索尚待迁移，不能将目标架构写成全部落地；未修改运行逻辑、数据库或服务配置。

### 2026-09-13 · Tavily 与公开网页独立代理

- **新增**：服务端 `WEB_HTTP_PROXY`，例如 `http://127.0.0.1:7890`；留空直连。仅用于联网搜索 API、Tavily Extract（若启用）与本地公开网页抓取，不设置全局代理，不改变普通教学模型、本地 RAG、Embedding、Workflow 请求。支持无账号密码的 HTTP/HTTPS 代理，不支持 SOCKS 地址。
- **当前本机配置**：搜索为 Tavily、正文提取为 local，代理使用本机已配置的 7890 端口。代理软件必须保持运行；部署到其他机器应配置那台机器可访问的代理或留空，不要照搬此本机端口。重启 Next.js 生效，无需迁移数据库或重启 RAG 服务。
- **安全**：网页仍校验公网 DNS 结果与每次重定向，CONNECT 连接到已校验 IP，Host/SNI 与证书验证保留原域名；内网地址、响应类型、2 MB 上限及阶段超时保护不取消。DNS 校验仍在本机进行，解析失败或代理 Fake-IP 返回非公网地址时会拒绝抓取，不会绕过校验。
- **错误提示**：搜索连接失败显示脱敏底层错误码，提示检查代理，不再只报笼统超时。未增加自动切换搜索供应商或三轮失败分支改造。
- **实测**：直接调用修改后的业务函数，Tavily 搜索约 2.2 秒返回 5 条 Oracle 结果；随后本地提取器经代理约 1.0 秒取得 Oracle 正文 8,484 字符。只读取公开网页，未调用 DeepSeek，未入库，不能视为入库/Embedding/课程生成全流程验收。
- **回归入口**：`scripts/web-proxy-smoke.mjs` 覆盖代理启停、API 域名限制、配置脱敏、CONNECT 固定 IP 与 TLS 原域名；加入 `npm run verify:learning`。
- **检查结果**：17/17 组回归、独立网页安全/提取测试、typecheck、lint 和生产 build 通过。未自动重启用户正在运行的服务。

### 2026-09-12 · 课后草稿、成绩恢复与完成数修复

- **新增答案草稿**：课后答题停止输入约 600ms 后保存；显示恢复中、待保存、已保存和失败状态。刷新后恢复已确认保存的答案及换行。切换课节/目标、提交评分前先保存；失败可重试或复制，不把草稿算作成绩、画像证据或任务完成。
- **版本与并发保护**：草稿按用户、课节和服务端题目指纹隔离。换题不套旧答案，未解锁课节不能写入；多页面冲突返回提示，不静默覆盖；网络响应丢失后的相同内容重试保持幂等。页面退出时尽力提交未保存内容，但没有离线队列，必须看到“已保存”才代表服务端已确认。
- **恢复成绩**：读取当前课节和题目版本最近一次已保存的评分、反馈及答案；刷新后仍能查看成绩并进入已解锁下一课。新修改的答案不会冒充已评分结果，前端评分缓存也按题目版本隔离；顶部等级使用恢复后的结果。
- **修复实测问题**：原完成数在服务器返回课程后又追加一次，造成一课通过显示两课，现按课节 ID 去重。草稿请求在浏览器里的原生 fetch 绑定错误已修复并加入回归；演示课程顶部不再显示已通过模型质量门禁。
- **数据库**：schema 17 新增 `lesson_answer_drafts`，不删除原表或历史课程。重启 Next.js 后首次访问数据库自动创建新表，无需改环境变量；原进程的数据库连接不会仅靠热更新补建表。
- **验证**：16/16 组隔离/模拟回归、数据库 schema 校验、typecheck、lint、生产 build 通过。另用独立数据库和显式规则演示账号完成浏览器验收：填写三题→自动保存→刷新恢复→评分→刷新恢复成绩→进入并生成第二课→第二课答案不串用→通过后即时显示 2/3。测试未调用真实 LLM，也未修改用户数据；这不是教学准确率或模型闭环效果评测。
- **本轮范围**：先修复可靠性，评测数据集按用户要求暂缓。初始诊断草稿、后台生成任务持久化与进程重启续跑仍未实现；详细边界见 [可靠性修复记录](docs/LEARNING_RELIABILITY.md)。

### 2026-09-12 · 学习评分可信度与审核失败恢复

- **正式评分不再回退估分**：Tutor 评分失败、空输出或字段不合法时，接口返回 `grading_unavailable` / HTTP 503，可重试；不写成绩、不更新掌握度、不完成任务、不解锁下一课。接口与数据库入口双重保护。初始诊断评分失败也不再按字数/关键词推断能力上下界；缺少模型配置不再等同于主动开启演示模式。
- **评分契约**：严格检查题目数量、唯一 questionId、分数范围与 maxScore、反馈文字；按 questionId 对齐结果而非数组位置。未作答不得分，真实 0 分保留，参考答案取服务端保存版本。格式错误只修复一次；评分写入前再次检查课程内容版本、题目及可用状态，拒绝将旧题结果写入已改变的课程。
- **审核失败与内容不合格分开**：无资料上下文时，语义审核异常也不会默认通过。正文及失败原因保存为待复核，不触发因服务故障造成的全文重写；重试复用正文与引用证据，仅重新审核，通过后才出题。复核发现真实内容问题时，仍使用原有有上限的修订流程。纯结构检查不再隐含语义检查通过。
- **前端与演示模式**：增加「正文已保存，等待语义复核」「保留正文，重试复核」及明确评分失败提示；旧成绩不冒充本次结果。显式 `LLM_PROVIDER=demo/rules/local` 的演示课程保留规则评分，但标注“规则演示估分”，不称为真实掌握度；演示语义检查标注未执行模型复核。原字体和字号不变。此阶段失败答案仅在内存中；后续已补课后草稿，见上方同日记录。
- **重复证据去重**：同一用户、同一课节、同一套题重复作答仍保存尝试与完成结果，但不再重复增加掌握度、证据数和置信度。第一次答错后订正可以通过任务，能力值仍需后续新题提供独立证据；这不是经过校准的知识追踪模型。
- **验证与边界**：新增评分完整性、审核失败恢复及实际 React 报告渲染回归，并强化隔离数据库闭环中的失败保护和重复证据测试。本轮测试使用模拟模型/隔离数据库，不代表真实教学准确率、RAG 增益或浏览器全流程已验收；没有修改用户历史成绩或课程数据。具体范围与后续验收见 [可靠性修复记录](docs/LEARNING_RELIABILITY.md)。
- **检查结果**：15/15 组回归、typecheck、lint 通过；生产 build 首次因沙箱禁止派生类型检查子进程报 `spawn EPERM`，获准在沙箱外重跑后完整通过。
- **生效**：重启 Next.js，无需新增环境变量或数据库迁移。历史报告不会自动重写；新发生的语义审核不可用可以走复核恢复入口。

### 2026-09-11 · 修复评分后下一课无法继续的入口

- **修复**：已解锁课节若在正文保存前生成失败，仍处于 planned 状态，原界面没有重试入口。现在为已解锁但尚未生成的课节显示「生成本节并开始学习」，沿用独立进度流；目录区分未解锁、已解锁待生成和生成失败，不再全部显示「待前一节通过」。
- **交互调整**：网页评分请求使用 `deferNextLesson: true`，先保存并返回成绩、任务状态和下一课解锁结果，不再等待下一课长时间生成。通过后显示「成绩已保存，进入下一课」，由用户在下一课启动生成并查看进度；不是后台自动任务，刷新后可从已解锁课节继续。旧客户端不传该字段时保留原同步行为。
- **保护**：服务端拒绝为 locked/archived 课节启动生成，避免绕过前端解锁限制；评分与下一课生成的失败互相隔离，不要求再次评分才能重试。
- **真实复现并修复恢复路径**：本轮真实模型生成中，正文修订通过后巩固题发生 `validation_failed`，导致课程不可用。原日志没有记录具体字段，不能断言当次原始格式；已增加字段级诊断、明确字符串/完整教学块 ID 约束，并保留代码换行。若错误仅为 `assessment_generation_failed` 且正文结构和语义均已通过，重试保留原文与资料，只补出题；出题再次失败保留旧内容，不重新跑全文生成。
- **实测边界**：对上述失败测试库进行只读 SQLite 备份，在副本中调用真实模型完成「只重试出题」，验证正文逐块不变、未新增正文生成调用、三道题保存、课节恢复 ready。此次全链路首次真实模型运行未一次通过，不能计作全部通过；隔离规则版则已通过评分、进度回写、解锁及实际下一课流式接口。未开展浏览器全流程验收。
- **验证**：新增评分快速返回且不调用下一课生成的接口回归；隔离闭环测试改为通过实际 `retry-lesson-stream` 路由验证进度与结果，并覆盖锁定课节拒绝生成。12 组回归、typecheck、lint 通过；完整生产 build 在获准沙箱外执行后通过。真实模型验证结果另记；没有改写用户学习数据。

### 2026-09-10 · 课程生成、修订和考核数据契约统一

- **修复**：完整的案例字段（scenario/steps/result/verification）可归一化为 worked_example，不再误报缺少普通正文；不完整案例仍进入有上限的格式修复。保留正文和代码中的换行及缩进，避免代码注释吞掉后续语句。
- **修复**：生成与修订复用同一内容归一化逻辑，采纳修订后的 evidenceRequirements；未返回要求时从实际练习推导，不再恢复已经删除的旧主题要求。当前课节的标题、目标、时长与最终内容同步；仅更新当前课节，不重排整个路线。
- **修复**：巩固题依据最终修订后的目标和完成要求生成；模型出题失败不再静默发布模板题。显式关闭 LLM 的规则 Demo 保留。已保存评分不会因下一课失败丢失。
- **新增**：`npm run verify:learning` 汇总 12 组隔离/模拟回归测试，覆盖传输、规划、内容契约、资料检索补充、重新生成、评分与下一课解锁。
- **验证**：12/12 回归测试、typecheck、lint 通过；真实模型重放此前失败输入成功（11 个教学块，结构检查通过）。独立数据库真实模型闭环通过：首课内容检查 88 分，错误答案 0 分不解锁，参考答案测试 100 分完成任务，目标进度 33%，下一课内容检查 100 分并 ready。这不是教学准确率评测，也不包含浏览器登录验收；未修改用户课程。
- **尚未通过**：真实联网探测返回 HTTP 200/completed，但只有 reasoning/message，没有成功的 web_search_call，不能视为真实搜索；错误提示已明确区分接口未执行搜索与网页抓取超时。生产构建编译成功，后续 TypeScript 子进程受环境 EPERM 限制，沙箱外验证未获批准，因此本次不宣称完整 build 通过。
- **生效方式**：重启 Next.js 后重试生成；现有失败报告不会自动改写，无需删除资料或迁移数据库。

### 2026-09-10 · 暂停网页入库前用途审核，收录后判断是否继续补充

- **修改**：`WEB_PAGE_REVIEW_ENABLED` 默认 `false`。抓到正文后直接执行现有解析、切片、入库和向量化，不再因 `page_uncertain`、用途审核输出 `validation_failed` 或缓存的目录/宣传分类阻止收录；复用已收录页面同样跳过该审核。设置为 `true` 可恢复原审核及手动目录展开逻辑。
- **保留**：用户/目标归属、公开 URL/SSRF 防护、网络超时和正文有效性检查。关闭用途审核不等于信任网页内容：收录后仍重新 RAG，由现有 LLM Evidence Review 判断证据充分性和 missingTopics；足够就停止，不足继续补缺口，最多三轮。默认关闭时不按目录分类递归抓取，也不把搜索摘要或模型回答当抓取原文入库。
- **进度与记录**：明确显示“网页用途审核已关闭”“收录不代表内容已验证”和“由 LLM 判断是否继续抓取”；`web_page_review` 阶段记为 `skipped`，不伪装成审核通过。审核策略纳入知识门禁缓存指纹，旧策略的失败结论不会阻止新策略下的重试；无需清空资料库或重新向量化已有文档。
- **验证**：隔离数据库/模拟网络覆盖默认跳过审核、缓存拒绝后重试、资料复用、目录正文收录、权限保护、开关开启的兼容路径，以及收录后 RAG、提前停止、三轮上限和策略切换后的缓存失效。typecheck、lint 通过；本次未调用真实模型、未抓取真实网页、未改动用户资料。重启 Next.js 生效。

### 2026-09-10 · 重新生成课程复用创建流程，不再固定兜底标题

- **新增**：课程页「学习路线」区域提供「重新生成课程」，读取原目标、完成标准、自评基础、背景、每周时间、目标日期，复用已有能力与诊断结果、资料范围和当前课节数量，无需再次填表。通过同一个 `generateCourseForGoal` 服务重新执行 Planner → 首课 Tutor → 内容检查 → 出题 → 保存，不仅重做旧标题下的正文；后续课节仍按考核逐节生成。
- **交互**：完整重生成前确认影响；进度弹窗区分「重新规划并生成课程」与「本节正文重试」，显示规划修复、资料判断、首课生成、检查和保存过程。页面显示课程版本号，成功后同步课程选择、目标卡片与本地缓存。原失败课节按钮改名「仅重试本节正文」，保留原课名与路线。
- **数据保护**：完整重生成使用新的课程/首课 ID，只有规划与首课检查成功后，才在同一事务内归档旧课程并启用新版本。旧课程、答题记录、任务关联、诊断和能力画像不删除；新路线完成进度从 0 开始，确认框明确告知。历史课节仍可通过原任务访问，历史成绩不覆盖当前路线进度；暂未提供单独的历史版本列表。失败或并发请求引用旧版本时不替换当前课程。
- **修复**：Planner 不再把不合格的课节数量、缺失标题/目标/能力关联或直接拼接目标原话的模板课名当成有效模型结果；课程骨架允许一次结构修复并报告进度。模型规划实际失败时保留目标并提示重试，不再静默保存模板路线。显式关闭模型时首次创建仍支持规则 Demo，但完整重生成需要有效模型规划；既有课程不会被自动重写。
- **验证**：新增 `npm run learning:regeneration:smoke`，在隔离数据库与模拟模型响应下覆盖原资料复用、已完成诊断复用、权限、NDJSON 进度、新课名、版本切换、规划/首课/事务失败回滚、旧任务和成绩保留、历史评分与当前进度隔离，以及规划格式修复。typecheck、lint、build 通过；本次未调用真实模型或修改用户课程，也未进行浏览器完整操作验收。重启 Next.js 后可自行测试，无需迁移数据库。

### 2026-09-10 · 最小学习闭环联调通过

- **实测**：隔离数据库中使用已配置的真实模型，完成三节课程路线、首课正文及语义检查、三道巩固题、两次评分、任务完成/掌握度/目标进度回写，以及下一课自动生成。首课与下一课内容检查均通过（92 分）；“不知道”测试答案为 0 分且任务未完成；参考答案测试为 90 分，任务完成、目标进度 33%、下一课 ready。
- **修复**：下一课生成异常或内容检查未通过时，不再将已成功保存的本节评分当成失败返回。接口保留 grade/program 并返回 nextLessonWarning，前端提示本节已保存、下一节可重试，避免用户重复提交本节答案。
- **新增**：`npm run learning:loop:smoke` 使用规则模式验证业务闭环；加 `-- --live` 运行真实模型版。认证在测试入口固定为测试用户，业务服务、任务创建处理器及 SQLite 事务真实执行；不是浏览器登录/操作全流程测试，也不是教学质量或评分准确率评测。联网在该测试内关闭，只验证宽松门禁的学习闭环，不修改现有用户数据。
- **验证**：规则版及真实模型版闭环通过；下一课失败保留评分的接口回归测试通过；typecheck、lint、build 通过。重启 Next.js 生效，无需数据库迁移。

### 2026-09-10 · 课程结构自动修复，优先跑通闭环

- **修复**：课程生成与内容修订遇到无效 JSON 或字段校验失败时，回传具体字段路径，最多自动修复一次。连接重试与格式修复分别计数，共用原有请求截止时间，不无限重试。
- **兼容**：正文可读取 body/content/text/explanation 或已有要点；未输出 evidenceRequirements 时，从实际练习与完成标准派生，不添加通用占位教学。
- **诊断**：进度显示具体结构错误；agent_runs 保存错误字段（成功修复标为“已修复”），失败报告不再只显示 validation_failed。两次模型输出的 Token 合并统计，不记录思维链或密钥。
- **测试入口**：`node --experimental-strip-types scripts/lesson-structure-smoke.mjs`；加 `--live` 仅读取最近失败课节输入并调用实际模型，不覆盖用户课程。保留内容质量检查与答题完成门槛。
- **实测**：真实失败输入经一次格式修复后成功生成 10 个教学块、2 项考核要求，基础结构检查通过。定位到 reflection 使用练习字段却被强制要求 body，现按其已有题目及完成标准映射为练习块。此结果不是完整学习—答题—任务完成的端到端验收。
- **验证**：typecheck、lint、build、结构修复和连接重试模拟测试通过。重启 Next.js 后重新生成即可；没有覆盖用户已有课程或修改数据库结构。

### 2026-09-10 · 知识门禁宽松模式

- **修改**：默认 `KNOWLEDGE_GATE_MODE=advisory`。检索/证据审核异常、关闭联网时资料不足，或三轮补充仍不足，不阻断教学，改用模型知识生成；`strict` 可恢复强证据门禁。显式指定教材仍保留范围约束。
- **修复**：复用此前资料不足记录时直接降级，不重复三轮联网；来源发生变化后再验证。降级清除旧 groundedContext 与来源引用，页面标注“模型知识生成 · 未经资料验证”。
- **保留**：课程结构、示例、练习与内容质量检查继续运行；模型调用失败仍报错。模型生成内容不写入 RAG，网页入库审核不放宽。课堂提问无已验证资料时同样提示未验证。
- **验证**：类型检查、lint、构建与门禁模拟测试通过；工作流 12 项通过，包含旧检索节点的宽松放行兼容。未重跑真实模型完整课程。重启 Next.js 与 Workflow 生效，无需迁移数据库。

### 2026-09-10 · 按知识缺口自动补充，最多三轮

- **修改**：Tutor 需要验证时先 RAG；不足则搜索具体知识正文并自动收录，每轮后重新 RAG/Evidence Review，充分即停止，最多三轮后返回 `knowledge_unresolved`。不再强制弹出教材选择，不自动展开目录或下载书籍附件；指定资料模式仍遵守用户范围。
- **修改**：每轮处理最多三个未处理候选，单项失败继续；已入库正文复用，轮次仅属于本次操作，用户主动重试不受历史累计额度限制。保留手动资料选择和上传入口。
- **前端**：创建目标和重新生成课程的进度中保留轮次、知识缺口、网址、正文审核、解析切片、向量化、失败原因及覆盖验证结果。长记录在固定高度区域滚动，百分比是阶段估计，不是模型思维链或下载字节进度。
- **修复**：用途审核回退时显示具体失败代码，区分正文下载失败与下载后的模型审核失败。本条替代此前“一轮补充、等待用户多选”的自动流程规则。
- **验证**：类型检查、lint、构建通过，工作流 11 项通过；三轮门禁、网页入库、用途审核及资料选择渲染 smoke 通过。外部服务使用模拟测试，未将其等同真实公网完整授课验收。重新生成结束后仍可展开查看上次详细执行记录。

### 2026-09-09 · 最小知识补充闭环与操作预算修正

- **修改**：首次 RAG → 不足按 missingTopics 搜索 → 用户选资料并收录 → 再 RAG 一次；仍不足返回 `knowledge_unresolved`，不继续自动联网。新开课由当前课节门禁检索，跳过重复的整目标检索。
- **修复**：移除跨操作累计 RAG/Web/页面扣额及共享 120 秒抓取预算。旧计数字段仅保留兼容，不再阻止换批、多选或重试。本条替代下方历史记录中的旧预算规则。
- **修改**：每次最多选 3 个来源；普通来源 1 页，目录最多额外 2 个同域相关子页，每页独立限时 60 秒。三个目录可分别抓取，不共用三个页面额度。
- **修复**：单项失败不阻断其他来源；目录部分成功仍可重试并复用成功正文；全部成功子来源加入目标资料范围，候选恢复优先显示用户最新搜索批次。
- **测试**：增加旧额度耗尽仍可操作、三个目录/九页、部分成功复用、独立重试和课节级门禁恢复用例。规则见 [Knowledge Gate](docs/KNOWLEDGE_GATE.md)，模拟测试不等同真实公网全流程验收。
- **验证结果**：typecheck、lint、build 通过；工作流 11 项通过；Knowledge Gate、网页收录、独立超时、候选搜索、选择界面渲染、Evidence Review smoke 通过。未重跑真实公网完整授课流程。启用需重启 Next.js 和 Python Workflow，无需重建向量库。

### 2026-09-09 · Knowledge Gate 真实接口联调修复

- **修复**：门禁研究 ID 原以 `knowledge-gate:` 开头，与 Python `WebAdvance` 必须以 `web-research:` 开头的校验不一致，实际搜索前即被拒绝。统一为 `web-research:knowledge-gate:<稳定缺口ID>`；增加模拟测试中的前缀契约检查。
- **修复**：搜索失败原因保留在门禁记录、进度消息和开课等待提示中，刷新后仍可读取，不再一律替换成“请选择资料”。统一搜索进度文案为一轮预算。
- **新增**：`node scripts/knowledge-gate-live.mjs --live` 使用真实 HTTP 接口创建独立测试账号/目标，检查空资料库触发搜索、候选返回、等待选择以及重放预算不变；报告保存于 `.runtime/knowledge-gate-live-*.jsonl`，不保存密码或 Cookie。此脚本不替用户选择来源，也不宣称完成课程生成。
- **实测**：修复后真实搜索约 39 秒返回 5 个候选；API 停在 `sources`，未抓正文。重复准备请求后预算仍为 RAG 1 / Web 1 / 页面 0。报告：`.runtime/knowledge-gate-live-1788923517087.jsonl`。类型检查、lint、门禁 smoke 通过。内置浏览器登录页的注册切换按钮未观察到响应，UI 全流程尚未验证，不把接口测试等同于页面通过。

### 2026-09-09 · 轻量 Knowledge Gate（新增 / 修改）

- **新增**：Tutor 的 `none / verify / current / source_required` 结构化知识决策；不保存思维链。仅对已验证正文的纯教学改述允许 `none`；课程生成、定义/API/精确事实至少验证，时效问题和指定教材由系统规则覆盖模型判断。无效输出保守使用 `verify`。
- **修改**：开课资料准备、课节生成、课堂提问接入门禁，复用 RetrievalRun 与原 Evidence Review。先本地 RAG，不足时仅按 `missingTopics` 搜索摘要；用户确认 → 正文用途检查 → 入库/切片/Embedding → 再 RAG/审核，不能拿搜索摘要直接授课。指定教材必须勾选“仅指定资料”，不会自动替换教材。时效审核要求真实原文中的版本/日期依据，不能仅靠相关性放行。
- **新增**：Schema V16 的 `knowledge_gates` 与 `knowledge_gate_candidates` 保存决策、证据、来源快照指纹、候选关联及预算。一个教学动作的知识缺口共用 RAG ≤ 2、Web ≤ 1、抓取 ≤ 3 页；执行前原子预扣，失败也计数，刷新不重置。资料未变化时维持 `waiting_for_sources`；补充后仍不足返回 `knowledge_unresolved`，不回退模型记忆硬讲。不同课节/不同提问是独立动作，不是整个用户只能搜索一次。
- **修改**：LangGraph 搜索摘要返回后直接等待用户，不立即再检索；空结果不自动搜索第二轮。等待资料不记成生成失败。课堂提问等待资料时打开原资料选择窗口，保存后重新发送保留的问题。
- **新增**：已选目录页只按标题/锚文本/URL 排序同源内部链接，抓最多 2 个相关正文子页（目录本身也占 1 页，因此整个缺口最多 3 页）。不递归、不跨站追书、不让 LLM 阅读全目录；目录页本身不入库作为教学证据。仍保留公网地址/DNS 校验和原抓取超时机制。
- **测试**：新增 `npm run knowledge:smoke`；扩展网页测试验证“目录 + 两个子页”、预算耗尽前拒绝 I/O；扩展证据测试验证时效依据不能虚构。隔离 SQLite、模拟 RAG/Web/模型验证分支，不代表真实搜索成功率或教学质量。Workflow 9 项、类型检查、lint、生产构建已通过，详见 [Knowledge Gate 实施说明](docs/KNOWLEDGE_GATE.md)。
- **启用**：当前工作区已执行 `npm run db:migrate` 并通过完整性检查；请重启 Next.js 与 Python Workflow。其他环境先执行迁移。MinerU/Embedding 不需要因本次代码修改重启。历史目标/课程/资料不删除；历史工作流节点保留兼容，旧动作缓存不自动重新运行。

### 2026-09-08 · 网页入库前用途筛选与缺口驱动搜索

- **新增**：网页正文抓取后、切片/入库/向量化前执行用途筛选。明显的纯链接目录用保守规则识别；其余页面仅向模型发送标题、标题列表以及首/中/尾正文样本（正文样本最多 3600 字符，不是 3600 Token），输出上限 600 Token。此检查不下载整站、不追踪书籍、不额外抓子链接。
- **分类**：teaching（实际讲解）、resource_index（书单/目录/资源入口）、promotion（宣传）、uncertain（不足以判断）。只有 teaching 放行，并要求提供抽样正文中真实存在的教学原句。入口页提示用户选具体章节或上传合法拥有的书籍正文；宣传页提示换源；不确定或模型不可用时不默认放行。
- **新增**：Schema V15 的 web_page_reviews 按用户、网址、标题、正文哈希、审核规则版本和模型配置缓存确定性分类结果。正文未变时不重复调用分类模型；不确定和服务失败不缓存。审核耗时、输入输出 Token 和缓存命中写入 web_page_review 阶段指标。字符抽样有遗漏风险，后续课程证据审核仍需保留。
- **修改**：重新收录已有网页时审核存储正文，不重复下载。历史已入库资料不会自动删除、重新切片或解除关联；本次拦截仅阻止新的处理，旧资料仍受后续课程证据审核约束。
- **修改**：课节补搜将现有审核的具体缺口与课节主题一起传给搜索规划；缺口变化时生成不同的稳定研究键。仍由用户选择候选，不让模型自行扩大目标，搜索轮数上限不变。
- **验证**：隔离数据库与模拟模型测试覆盖抽样大小、引用原句校验、规则目录筛选、宣传分类、缓存隔离/失效和不确定重试；集成回归验证被拒绝网页不进入切片/入库/向量化。未宣称真实网页分类准确率或已完成课程生成。类型检查、lint、数据库 Schema 校验通过。

### 2026-09-08 · DNS 超时拆分与网页连接重试

- **修复**：DNS 单独允许 20 秒，解析完成后再启动 TCP/TLS 的 10 秒时限，解决 DNS 约 11 秒时被旧的合并时限提前中止的问题。首次响应仍为 30 秒；所有阶段、重定向和重试共用单网页 60 秒截止时间，批次累计抓取 120 秒不变。
- **新增**：公开网页 GET 遇到 ECONNRESET、ETIMEDOUT、ECONNREFUSED、EAI_AGAIN、ENETUNREACH 或连接阶段超时时，最多自动重试一次；多地址情况下尝试另一条已校验的公网地址。重新解析后仍校验所有地址、保留 Host/SNI 与证书校验，不更改代理、DNS 或系统网络设置。
- **边界**：HTTP 拒绝访问、私网地址、证书错误、解析错误以及总预算耗尽不自动重试；重试只作用于网页读取，不重复 LLM 搜索、资料入库或向量化。搜索与推荐逻辑未改。
- **验证**：测试覆盖慢 DNS 超过连接时限仍可完成、DNS 独立超时、连接重置后一次重试、HTTP 403 不重试、原有页面/批次截止时间。类型检查与 lint 通过。使用真实 extractPublicWeb 只读提取原失败页面：JavaGuide 路线页约 648 ms、39512 字符；GitHub dunwu/java-tutorial 约 1339 ms、14817 字符。未执行入库/向量化，此次成功不等于保证网络持续稳定。
- **启用**：Next.js 开发服务热更新后生效；如页面仍显示旧的合并超时说明，重启 Next.js。无需为此重启 Python Workflow。

### 2026-09-08 · 资料选择弹窗布局修复

- **修复**：移除旧的固定六行网格，将资料选择弹窗拆为固定标题、中间统一滚动内容、固定底部操作区。中间区域允许收缩且独立滚动，联网候选不再溢出叠到资料列表或按钮上。
- **修改**：联网区正文/输入使用现有资料弹窗的 10px 字号体系、分区标题 12px，不修改全局字体和页面字号。底部按钮允许窄窗口换行。
- **修改**：已有资料库单独折叠分区并标明不是本轮搜索候选，默认收起；展开后跟随同一个内容滚动区，不再形成空白大列表和嵌套滚动。
- **验证**：组件渲染和结构回归、typecheck、lint；未完成真实浏览器布局截图验收。

### 2026-09-08 · 等待资料选择窗口不再一闪而过

- **修复原因**：此前只增加手动资料入口，创建目标/继续学习仍将 sources 等待状态抛成错误；重试课程也只显示异常后关闭进度窗口，并未自动展示候选。
- **修改**：创建目标、继续学习、课节重生成和自适应诊断结束等待资料时，自动打开持久停留的资料选择窗口。重生成/诊断通过结构化 needs_sources 事件区分等待与失败，不靠错误文案猜状态。
- **修改**：选择窗口通过 Portal 放到 document.body，避免父容器裁切。用户显式「稍后选择」才关闭；自动弹出的窗口不允许点击背景误关。收录过程中禁止关闭或保存继续，防止提前关闭选择界面。
- **修复**：「保存并继续课程」对目标工作流发送 sources_updated 恢复事件，而不是再次查询旧的等待状态；课节重试则继续原课节生成。
- **验证**：新增真实 React 组件渲染测试验证等待弹窗、补充要求、搜索和继续按钮；类型检查及相关回归通过。未完成登录后的浏览器全流程点击验收。

### 2026-09-08 · 分阶段抓取超时与服务端批次预算

- **修改**：默认本地网页抓取使用 DNS/连接/TLS 握手合计 10 秒；连接成功后等待完整响应头最多 30 秒；从开始解析域名到正文下载结束，单网页整体最多 60 秒，重定向共用同一整体截止时间。超时会中止底层请求并清理计时器。
- **新增**：错误区分 `connect_timeout`、`first_response_timeout`、`page_timeout`、`batch_timeout`，保留公开的网络错误码；不再将所有异常笼统归为“抓取失败或超时”。
- **新增**：`import-batch` 服务端批次接口，每批 1–3 个不同且属于当前用户/目标的候选，先验证全部选择再开始操作。各网页抓取共享累计 120 秒预算，切片、入库、向量化耗时不占抓取预算，故整个处理流程可能超过 120 秒。失败仍消耗已用预算，耗尽后不再发起新下载。只处理用户选择的来源，不自动抓取未选网页。
- **修改**：前端限制最多选 3 项，通过 NDJSON 实时展示批次进度与逐项结果。一个来源失败继续处理剩余已选项，成功项可复用。页面断开不代表服务端已取消；恢复后应先查看资料库，批次不是可跨进程恢复的持久后台任务。
- **范围**：精细的连接/首次响应计时用于默认 `WEB_EXTRACT_PROVIDER=local`。显式选用 Tavily 时只能约束提取 API 请求和批次预算，无法控制其内部网页连接阶段。
- **验证**：短时钟模拟覆盖正常响应、连接/首次响应/下载超时、预算耗尽；来源测试覆盖三项限制、权限预检和单项失败继续；typecheck、lint 通过。真实只读抓取 GitHub JavaGuide 约 936 ms、Oracle 教程约 334 ms 成功，不调用 LLM、不写用户资料；这是当次网络结果，不保证后续一直可达。
- **启用**：本次修改属于 Next.js 服务，无需为此重启 Python Workflow；开发服务通常热更新，生产部署须重新构建并重启网页服务。下方旧记录的“本地抓取 30 秒”和“前端逐项独立导入”已被本条替代。

### 2026-09-08 · 摘要预览、多选资料与反馈换批

- **修改**：课程资料不足时仅搜索候选摘要，暂停等待用户确认，不再自动抓取首个推荐网页。旧的待执行自动收录动作也会被暂停。计划目标及课程页的「资料范围 → 联网补充资料」可查看最近一批已保存候选。
- **新增**：候选标题、来源链接、搜索摘要及多选框；点击「使用所选资料」后才逐项抓取、切片、入库、向量化。单项失败显示在对应候选下，继续处理其余选择；成功项本次不重复处理，失败项可单独重试。页面重开后再次收录仍复用已有正文。
- **新增**：最多 1000 字的补充要求输入框，可说明程度、语言、资料形式或上一批不满意的原因。「按补充要求换一批」将原主题和最新要求共同传给查询规划，创建新研究记录，不沿用旧查询断点；每批最多两轮搜索、最多五个候选。未承诺结果必定不重复，不满意时应具体描述需要改变的方向。
- **修改**：新研究跳过额外的候选 LLM 评分，直接使用搜索服务的摘要供用户选材；仍保留 Pydantic AI 查询规划和 LangGraph 搜索上限/等待节点。正文入库后的教学相关性审核不变，搜索摘要不能充当教学证据。
- **新增**：只读候选恢复接口按当前登录用户和目标校验权限，按最近成功搜索的时间范围读取本批候选，避免将多轮结果混在一起。字体和字号继续继承原页面。
- **使用**：收录完成后保存资料范围，再点「继续学习」或「重新生成本节」。多选目前通过前端逐项调用已有收录接口，不是持久化批量后台任务；请等待本次处理结束再关闭页面。新 Python 搜索图需重启 Workflow 生效。
- **验证范围**：类型检查、lint、摘要阶段不自动收录、反馈传递、等待选择时不生成课程、资料所有权及去重回归；未用真实模型重新生成课程，未进行浏览器点击验收。之前的自动首条收录测试方案已调整为显式确认测试。

### 2026-09-08 · DeepSeek 联网真实验证与本地正文提取

- **修改**：默认 `WEB_SEARCH_PROVIDER=deepseek`、`WEB_EXTRACT_PROVIDER=local`。搜索调用 DeepSeek 原生 `web_search`，正文使用 Readability + Turndown 本地提取为 Markdown，不再要求 `TAVILY_API_KEY`；Tavily 仅作为显式可选配置。下方旧记录中的 Tavily 必填阻塞已解除。
- **新增**：网页提取校验公网 DNS 并固定连接地址，每次重定向重新校验；拒绝私网、凭据 URL、HTTPS 降级以及不支持的内容类型。限制响应体 2 MiB、最多 3 次重定向、总超时 30 秒，不执行网页脚本，不携带登录信息。适合公开静态正文，不支持登录页或依赖脚本渲染的页面。
- **修复**：Workflow 的查询规划和候选评估在官方 DeepSeek 上显式关闭 thinking，兼容 Pydantic AI 强制结构化输出工具；Tutor 正文生成/修复与教学证据审核也针对官方 DeepSeek 关闭 thinking。其他供应商不传此私有参数。Workflow 失败节点可恢复执行，用户等待节点仍保留中断。
- **修复**：DeepSeek 搜索区分网络失败、超时、JSON 格式错误；仅明确建连前失败自动重试一次，共用原始 120 秒截止时间，不重放可能已计费的请求。
- **修复**：教学审核后恢复 `retrieval.evidence_bundle` 阶段名，避免重复写入 `retrieval.teaching_review` 导致计时表唯一键冲突。
- **新增**：`scripts/web-learning-live.mjs --live` 使用独立测试账号及临时 Workflow，串联真实搜索、入库、证据审核与课程生成断言；`.runtime/web-learning-live-*.jsonl` 保留阶段结果，脚本结束只关闭自己启动的临时进程，不关闭现有 8030 服务。该脚本调用真实模型，产生费用和测试数据；运行方式：`node --env-file=.env.local --experimental-strip-types scripts/web-learning-live.mjs --live`。
- **真实验证**：测试目标 `210e4705-9430-4bf2-9cf7-068bcc026bb9` 找到 5 个官方候选，Pydantic AI 自动选择 Oracle Primitive Data Types 页面，正文收录及向量化成功（sourceId `760474d5-dd3d-4934-a303-85927291fc7a`）。修复计时冲突后，教学审核保留 4 个片段但判定不足：缺少引用类型系统讲解和两类类型的对比示例。未进入课程生成，不宣称完整闭环已通过。
- **待完善**：自动研究当前只收录一个推荐来源；若重新审核仍不足，需要按缺失主题有界补充其他正文，再审核。不得仅为通过测试降低证据门槛。旧课程报告不自动重写。
- **验证**：typecheck、lint、DeepSeek 搜索模拟回归（含安全重试）、本地正文提取回归、网页来源隔离回归通过。Workflow 回归覆盖恢复、搜索预算和供应商参数隔离。正式使用修改后的 Python 代码须重启 Workflow；本次临时测试不替代用户服务重启。

### 2026-09-08 · 联网运行配置与本次失败提示

- **诊断**：实际资料审核已拒绝 Agent 资料用于 Java，未进入新课程生成。网页本地配置缺少 WORKFLOW_SERVICE_URL；运行中的 8030 服务健康，但 query_planner=rules。当前还未配置正文提取所需的 TAVILY_API_KEY。
- **修改**：本机 .env.local 接入 8030、启用自动联网和工作流模型；启动脚本读取白名单配置，WORKFLOW_LLM_* 缺省时复用已有 LLM_*，不输出密钥。需要重启网页和 Workflow，已有服务不会自动读新环境变量。
- **修复**：重试失败原因同时显示在当前课节质量报告上方，并注明下方报告属于旧版本，避免把新一轮资料准备失败误读成旧模型空回答。
- **待用户配置**：TAVILY_API_KEY 未填写，不能完成网页正文提取入库。不自动申请账号或购买搜索服务。

### 2026-09-08 · LLM 审核教学检索证据

- **新增**：教学检索返回前调用 LLM，根据真实目标、课节查询和证据正文审核主题相关性与覆盖缺口；向量近邻和数量长度不再足以放行。只保留审核返回且属于候选集的片段 ID，不相关片段不传给导师。用于所有 lesson_generation 检索，课堂问答暂不改变。
- **新增**：审核结论、理由、缺失主题、耗时与用量写入检索 filters 和 retrieval.teaching_review 阶段指标。审核调用失败、格式非法、返回未知片段或存在覆盖缺口时不放行；资料不足由现有流程联网补充并重新审核。审核器使用现有 LLM 配置，未启用模型也不会假装审核通过。
- **修改**：进度显示教学审核结论。旧的未审核快照不能直接作为新教学依据，需重新检索；历史课程和报告不改写。本轮没有改变模型思考模式配置，也不保证 LLM 判断绝对正确。
- **测试**：scripts/evidence-review-smoke.mjs 使用模拟 LLM，覆盖不相关、非法引用、有缺口、通过和调用失败；不代表真实模型准确率评测。

### 2026-09-07 · 本节重试和后续课节自动补充资料

- **修复**：共享课程正文生成入口接入 `resolveLessonEvidence`，重新生成本节、导师考核后的下一节同样执行“本地检索 → 不足时调用研究工作流 → 正文收录 → 再检索 → 生成”；已有证据充分时不联网，模型生成超时不会触发搜索。
- **新增**：重试进度窗口持续显示资料来源和原因。自动补充复用现有 Agent 推荐与收录校验，每节/查询使用稳定研究 ID，重复点击不会无限新建研究；研究工作流最多两轮搜索、收录一份推荐正文。没有推荐或补充后仍不足会停止，用户可手动添加资料再试。
- **边界**：默认启用 WORKFLOW_AUTO_WEB，需要 WORKFLOW_SERVICE_URL 与研究、搜索、正文提取配置；缺少服务会明确提示。显式关闭自动联网保留原来的资料门槛行为。课节重试整体仍不是持久化后台任务，只有联网研究部分有 checkpoint。
- **验证**：`node --experimental-strip-types scripts/lesson-evidence-smoke.mjs` 覆盖本地充分、补充成功、仍不足、缺少服务、联网失败、关闭联网、检索故障七种场景，使用模拟依赖，不修改学习数据或调用真实 LLM。

### 2026-09-07 · 模型调用失败不再伪装为引用缺失

- **诊断**：截图对应生成记录为首次 fetch failed（10.2 秒）、修订超时（120 秒）、再次 fetch failed（10.5 秒），输入包含 4 条证据。受限环境探针得到 UND_ERR_CONNECT_TIMEOUT，正常权限短请求成功；不能把沙箱代理配置当作用户网络配置，更不能从旧日志断言唯一网络根因。
- **修复**：结构化模型调用记录脱敏的底层错误码；只对明确建立连接前的临时故障重试一次，共享原始总超时，不重放已超时生成或不确定是否已执行的请求。不修改用户代理、密钥或证书设置。
- **修复**：正文生成或修订调用失败后保存 generation_failed 及单独服务错误报告，停止模板质量检查与后续修订；不再产生误导性的 7 条 source_missing。故意禁用模型的 demo 模式保持原行为。旧报告不改写，新尝试使用新逻辑。

### 2026-09-07 · 质量详情组件运行检查

- **修改**：质量详情拆到 `app/lesson-quality-details.tsx`，课堂显式导入，避免依赖同文件热更新中的新增局部组件。截图报错时磁盘定义存在，尚不能确认热更新是唯一根因。
- **新增**：`node scripts/quality-details-render-smoke.mjs` 实际执行 React 渲染，覆盖失败报告展开、通过报告折叠、缺失报告，以及问题描述/教学块/修复建议显示；不调用 LLM、不修改用户学习数据。

### 2026-09-07 · 展示课程质量问题详情

- **新增**：课堂显示当前版本的完整质量问题列表：需修复/改善建议、具体描述、涉及教学块标题、修复建议、问题编号与检查时间。失败课程默认展开，通过的课程可折叠查看；不调整原有字号。
- **修改**：课程读取接口提供精简 `qualityReport`，仅包含检查结论和问题，不向前端发送内部生成元数据或课后题答案。已有保存报告直接可读，无需重新调用 LLM；重新生成后更新为最新版本报告。
- **边界**：没有报告会明确提示缺失；语义检查的旧数据只有布尔值，因此 false 展示为“未通过或尚未执行”，不会冒充已经评审。这里显示的是课程内容质量，不是学生考核成绩。

### 2026-09-07 · 本节重新生成实时进度

- **新增**：`retry-lesson-stream` 返回正文生成、质量检查、最多两轮定向修订、保存结果的 NDJSON 阶段进度；原 JSON 接口保留。`materializeNextLesson` 支持进度回调。
- **修改**：课堂重试显示居中原生模态窗口、阶段进度和用时，沿用原有字体；生成期间页面其他控件不可交互，刷新/关闭页面触发浏览器离开提醒。正常完成或报错后解除限制，百分比是阶段进度而非实时 token 比例。
- **边界**：原先切换功能会卸载课堂组件、丢失前端状态，但不代表服务端被取消。这次重试仍为请求内执行，不是持久化后台任务；断连后进度写入不会主动抛错中止业务，但进程重启/部署超时仍可能中断。强制离开后应先重新读取课程，不能承诺自动续跑。
- **验证**：TypeScript 类型检查与 ESLint 通过；未发起真实 LLM 重新生成，本轮不宣称已验证模型调用耗时或生成质量。

### 2026-09-07 · 课程资料不足时自动联网补充

- **新增**：LangGraph 课程流程增加 `supplement_web` 节点。默认 `WORKFLOW_AUTO_WEB=true`：先检索目标资料库，证据不足才自动调用已有研究流程；最多两轮搜索请求、自动收录一份 Agent 推荐网页正文，再次检索后决定生成或等待用户补充。失败/无推荐/再次不足时暂停，不静默退回无依据生成。手动研究入口保留。
- **新增**：父流程外部动作与研究 run 使用稳定关联 ID，恢复时复用研究 checkpoint / 已执行动作；每个课程流程只自动补充一次，用户补充资料后的恢复不会重置搜索预算。自动选择只接受 Agent 推荐，不把规则模式的任意第一条搜索结果直接收录。
- **修改**：创建目标与诊断完成后的课程准备窗口保留“课程资料来源与原因”，解释已有资料可用、无资料、未匹配、证据过少、自动联网和正文入库等状态；沿用原有字体样式。资料库可能包含以前收录的网页，“本地”指已入库，不指从未来自网络。
- **修改**：课程修订提示明确是 AI 课程内容检查，不是学生成绩；显示前 3 个具体问题及第几轮修订（最多 2 轮）。完整 issues 仍保存在课程质量报告中。
- **边界**：自动联网要求启用并重启 Workflow 服务及网页服务，研究 Agent/搜索/正文提取需要各自有效配置（当前正文提取仍用 Tavily）。关闭 `WORKFLOW_AUTO_WEB` 时保留原有 `WORKFLOW_REQUIRE_SOURCES` 行为。未配置 Workflow 的旧业务路径不执行此自动联网流程。基础充分性判断仍为数量/长度门槛，不代表知识覆盖或真实性已验证；已有课程不自动重建。
- **验证**：7 项 Python 工作流测试通过（含本地充分不搜索、自动补充后继续、补充失败暂停及 checkpoint 恢复）；`scripts/auto-web-research-smoke.mjs` 验证自动选推荐、稳定 run 恢复、无推荐不收录和归属校验。TypeScript、Lint、生产构建通过。本轮使用模拟联网响应和隔离测试库，未运行真实付费搜索或验证真实生成质量。

### 2026-09-07 · 本地 RAG 一键启动

- **新增**：双击根目录 `start-rag.cmd`，或在项目目录执行 `npm run rag:start`，依次启动 MinerU（6000）、RAG ingestion（8010）、Embedding（8020）。不启动网页及 workflow 服务，不自动安装依赖或下载模型。
- **新增**：`scripts/start-rag-stack.ps1` 检查路径、验证服务健康状态、复用已启动服务；端口被其他程序占用或已有服务不健康时明确报错，不杀进程。后台隐藏运行，日志保存在 `.runtime/rag-stack/`，显示各服务启动耗时。启动失败时保留已经启动的服务供排查。
- **配置**：MinerU 使用 `G:\MinerU\.venv\Scripts\mineru-api.exe`；RAG 使用自己的 `.venv`；Embedding 默认复用 MinerU Python。只读取 `.env` / `.env.local` 中白名单内的 MinerU/Embedding 配置（后者优先），不会执行文件内容或打印密钥。本地 MinerU 地址固定为 `http://127.0.0.1:6000`。网页仍需配置 `RAG_INGESTION_URL=http://127.0.0.1:8010` 和 `EMBEDDING_SERVICE_URL=http://127.0.0.1:8020`。
- **说明**：Embedding 健康不代表模型已加载，BGE/Qwen 按首次请求懒加载；服务启动检查不等于 PDF 解析或向量推理成功。可执行 `npm run rag:start -- -CheckOnly` 仅检查路径及端口，不启动服务。
- **修改**：忽略本地 `.runtime/` 日志目录，不提交到仓库。

### 2026-09-07 · 三层研究流程与 DeepSeek 原生搜索

- **新增**：独立 LangGraph web-research 图：Pydantic AI 拟定查询 → 外部搜索动作 → Pydantic AI 评估候选 → 无结果/无推荐时最多补搜一轮 → 等待用户选择 → 外部收录动作 → 完成。每轮收录一份，可新建下一轮。
- **新增**：Next.js 根据用户/目标注入可信上下文并执行 Tool，Schema V14 `web_research_runs / web_research_actions` 保存研究 ID 和动作结果；Python SQLite checkpoint 保存等待点。界面提供“恢复研究”“结束本轮”和推荐理由，浏览器本地仅保存研究 ID；恢复时重新校验归属。
- **修改**：默认 WEB_SEARCH_PROVIDER=deepseek，调用官方 /responses 并强制 web_search；只接受 completed 响应且存在 completed web_search_call。候选从回答 JSON 提取，仍须正文收录验证，不能当作已认证来源。Tavily 可通过配置保留。
- **边界**：最多两轮指外层请求，不是 DeepSeek 服务内部搜索次数（官方服务可内部续推）。当前 Pydantic AI 负责结构化查询/推荐决策，LangGraph 通过受控动作执行 Next.js Tool，并非 Agent 无限制自主工具循环。未配置 WORKFLOW_LLM_* 时使用明确标识的规则查询且无推荐，不伪装为模型判断。
- **边界**：正文提取仍使用 Tavily Extract，因此 DeepSeek 搜索本身无需 Tavily Key，但“收录”仍需 TAVILY_API_KEY；尚未实现其他抓取后端。已缓存动作重放复用结果；外部调用完成但缓存落库前进程退出仍可能重发搜索，不宣称付费请求 exactly-once。同用户目标请求在单进程内串行，多实例分布式锁尚未实现。

启动：配置 WORKFLOW_SERVICE_URL 并运行 `npm run workflow:service`；启用研究员需 WORKFLOW_LLM_ENABLED=true 及 WORKFLOW_LLM_BASE_URL/API_KEY/MODEL。在 `.env.local` 配置 WEB_SEARCH_PROVIDER=deepseek、DEEPSEEK_API_KEY、DEEPSEEK_SEARCH_MODEL=deepseek-v4-flash（官方 LLM_BASE_URL 可复用 LLM_API_KEY），重启 Next.js 与 workflow service。

验证：4 项 Python 图测试覆盖原主图、研究预算、重启恢复和无效选择后继续；DeepSeek 模拟响应测试覆盖真实搜索调用标记、未完成响应、URL 过滤和凭据隔离；网页入库隔离测试通过。真实模型联网与推荐质量尚未联调。

### 2026-09-07 · 联网资料补充 MVP

- **新增**：目标“资料范围 → 联网补充资料”，输入关键词、预览最多 5 个候选、填写资料用途并逐个收录；界面显示忙碌状态和耗时，沿用原字号。
- **新增**：Tavily Search / Extract 适配器与 `searchWebTool / ingestWebTool`。前端只传目标和候选 ID，服务端注入用户身份；搜索摘要不作为教学正文。搜索查询和选中的 URL 会发送给 Tavily，个人资料全文不会发送给搜索服务。
- **新增**：Schema V13 的 `web_search_candidates / web_source_imports`，候选归属隔离、URL 到资料映射与收录时间；同用户重复收录复用资料，学习时不自动抓取。Markdown 原文进入既有 SourceVersion、父子切片和目标范围链路。
- **新增**：自动尝试 Qwen3 向量化，失败保留已入库正文及 FTS5，前端提示到资料库重试。搜索、正文提取、解析入库与向量化耗时写入 pipeline metrics。
- **安全边界**：应用服务器只请求固定的 Tavily API，不直接请求用户网页，不跟随 API 重定向；拒绝 IP、凭据、非 HTTP(S)、内网后缀与非标准端口。网页抓取与站点重定向由 Tavily 执行，本地没有实现通用爬虫或 DNS/连接地址绑定。
- **暂未实现**：Agent 自主搜索循环、手动 URL 导入、正文收录前预览、来源可信度自动验证、实时逐阶段流式进度。当前由用户主动搜索和选择，收录不自动重生成旧课。

配置：在 `.env.local` 添加 `TAVILY_API_KEY=你的Key`，重启 Next.js。搜索与正文提取使用 [Tavily 官方接口](https://docs.tavily.com/documentation/api-reference/endpoint/search)，Key 不发送到浏览器。已有 ingestion/embedding 服务沿用现有配置。

验证：`npm run web:smoke` 使用模拟提供商和隔离数据库，覆盖候选不直接入库、用户隔离、危险地址、正文收录后检索、重复请求只抓取一次与向量化降级；RAG 回归、Schema V13 校验、typecheck、lint 和生产构建通过。真实 Tavily 联调需配置有效 Key，当前不宣称已验证真实搜索结果质量。

### 2026-09-07 · RAG 证据接入 Tutor（阶段 D 首轮）

- **新增**：GroundedTutorContext 从归属校验后的 RetrievalRun 读取正文并校验快照哈希；工作流首课复用检索，独立入口及后续课节按本节目标检索。
- **修改**：生成、语义复核、修订使用同一证据；逐块 sourceChunkIds 缺失或越界阻止发布，语义复核另行检查实际支持关系。模型复核不可用时不放行来源课程。
- **修复**：lesson_block_sources 只保存每块实际引用与哈希，不再把整节引用挂到所有块。复用 Schema V12，课程 JSON 增加检索 ID 和有限来源信息。
- **新增**：课程页默认收起“查看本节资料依据”，展示标题、章节、页码、最多 600 字摘录和对应教学块；沿用字号。
- **兼容**：无资料 auto 范围且未强制来源时保留 unverified Demo；selected 范围、存在候选资料或强制来源时，证据不足返回 source_insufficient。旧课程不自动重生成。
- **暂未实现**：教学策略独立版本目录、逐概念覆盖、题目来源快照链、课堂提问 RAG；Tutor SubAgent 延期。

验证：来源工具隔离回归覆盖权限、缺失/伪造引用、有限摘录和不可变快照；typecheck、lint、生产构建通过；独立测试数据库的 V0.4.3 API 回归通过编程、表达、历史三类规则课程。尚未验证真实 LLM 来源课程质量，不宣称教学效果提升。下方 9 月 1 日“EvidenceBundle 尚未进入 Tutor”是历史状态，以本条为准。

后续每次功能、接口、数据结构或运行方式发生变化，都必须同步更新本节。标记含义：`新增` 表示新能力，`修改` 表示既有行为变化，`修复` 表示缺陷修正，`暂未实现` 表示已经确定但尚未落地的边界。

### 2026-09-01 · LangGraph / Pydantic AI 最小编排层（已实现 MVP）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 增加独立 FastAPI workflow service：LangGraph 使用 SQLite checkpointer 保存 `thread_id`，显式中断外部业务动作、等待诊断和等待资料；Pydantic AI 用严格输出模型生成资料检索计划，未配置工作流 LLM 时使用经过同一模型校验的确定性计划 | `services/workflow/` |
| **新增** | 数据库升级到 Schema V12，新增 `workflow_events`；Next.js 保存节点进度、等待状态、动作请求和动作结果，副作用结果按幂等键复用；若进程在 RetrievalRun 已落库但动作结果尚未缓存时退出，可按工作流键恢复该检索摘要 | `lib/db/schema.ts`、`lib/db/workflows.ts`、`lib/db/retrieval.ts` |
| **修改** | `/api/learning-program` 在配置 `WORKFLOW_SERVICE_URL` 后改由持久化图推进“读目标 → 能力地图 → 可选诊断 → RAG 检索 → 课程生成”；浏览器仍只访问 Next.js，用户身份、所有权、业务事务和实际 Tool 调用不下放给 Python | `lib/workflow/`、`app/api/learning-program/route.ts` |
| **修改** | 自适应诊断完成后可恢复同一工作流，再继续检索和课程生成；未配置 workflow service 时仍走原 `prepareGoalLoop`，不影响当前本地 Demo | `lib/learning-loop/adaptive-diagnostic.ts`、`app/api/diagnostics/route.ts` |
| **新增** | 增加 `npm run workflow:service`、`npm run workflow:test` 与 Python 单元测试，提供统一的本地启动和回归入口 | `scripts/start-workflow-service.ps1`、`services/workflow/tests/`、`package.json` |
| **暂未实现** | 当前 EvidenceBundle 只进入编排与资料充分性分支，尚未作为 `GroundedTutorContext` 传入 Tutor；质量修复循环仍封装在现有 TypeScript 课程动作内部，Planner/Tutor/Examiner 也尚未全部迁为 Pydantic AI 节点，因此课程仍不能标记为 `grounded` | `docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md` |

验证结果：Python 的普通流程重启恢复、诊断与资料双重 interrupt 两项测试通过；真实 Next.js + FastAPI 隔离数据库回归中，初学者目标生成 5 节课程，非初学者目标停在自适应诊断；重复恢复后仍只有 1 份诊断，`load_goal / ensure_skills / prepare_diagnostic` 动作各完成 1 次。`typecheck`、`lint`、Schema V12 校验与生产构建均通过；实际数据库已幂等迁移到 V12，保留 1 份资料、5,213 个 Child 和既有学习数据，`integrity_check=ok`。

### 2026-09-01 · 目标资料范围与 Agentic RAG Tool 核心（已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 数据库升级到 Schema V11；每个目标支持 `auto / selected` 两种资料范围。`auto` 默认检索当前用户全部可用资料并保存显式排除项，`selected` 只检索明确勾选的资料；旧目标缺少学习画像时会补齐默认画像 | `lib/db/schema.ts`、`lib/db/goal-sources.ts`、`lib/db/goals.ts` |
| **新增** | 长期目标卡片增加“资料范围”入口，可切换自动范围与严格范围；新增登录保护的 `GET/PUT /api/goals/[id]/sources`，服务端验证 Goal、Source 同属当前用户，客户端不能提交 `userId` | `app/goal-source-scope.tsx`、`app/dashboard-client.tsx`、`app/api/goals/[id]/sources/route.ts`、`app/globals.css` |
| **新增** | 实现目标级 `search_knowledge_base` Tool 核心，同一个契约支持 `lesson_generation` 与 `classroom_qa`。Agent 可见输入只包含查询、模式、模型、条数和证据预算；用户、目标、课节、能力与用途由服务端上下文注入 | `lib/knowledge/search-tool.ts`、`lib/db/retrieval.ts` |
| **新增** | 检索会跨目标允许的全部资料执行：`auto` 在目标范围内所有 active Chunk 都已完成所选模型向量化时使用精确向量检索，否则使用 FTS5；向量查询失败时自动模式可降级到 FTS5。结果执行 Parent 去重、单资料上限、Token 预算和基础来源充分性判断 | `lib/db/retrieval.ts`、`lib/knowledge/embedding-profiles.ts` |
| **新增** | 每次检索固定实际使用的 SourceVersion、ChunkSet、Child、Parent 展开文本与哈希，并写入 RetrievalRun、Item 和 PipelineRun 阶段计时；增加登录保护的 `/api/rag/search` 调试/未来 BFF 接口 | `lib/db/retrieval.ts`、`lib/db/pipeline-metrics.ts`、`app/api/rag/search/route.ts` |
| **新增** | 增加隔离临时数据库回归，覆盖自动范围、显式排除、严格指定、Source/课节/能力上下文跨用户隔离、跨资料 FTS5、证据预算、旧目标兼容和检索审计快照 | `scripts/v044-rag-tool-smoke.mjs`、`package.json` |
| **暂未实现** | 本条记录创建时 Tool 尚未进入工作流；现已由上方 LangGraph MVP 在课程生成前调用，但还没有把 EvidenceBundle 传入 Tutor 或课堂提问 UI，因此仍不能称为课程已经 grounded | `docs/IMPLEMENTATION_PLAN_V044.md`、`docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md` |

验证结果：`typecheck`、`lint`、Schema V11/FTS5 校验、`npm run rag:tool:smoke` 和 Next.js 生产构建已通过。现有数据库已幂等迁移到 V11，`integrity_check=ok`，仍保留 1 份资料和 2,038 条向量，未重算或删除 PDF、切片、Embedding 与学习目标数据；冒烟测试只使用系统临时目录中的独立 SQLite，并在结束后删除。

### 2026-09-01 · LangGraph/Pydantic AI 接入顺序调整（仅文档冻结）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 将最小框架迁移从“来源课程完成后的 V0.4.5”提前到“目标资料绑定与本地 RAG Tool 完成后、Tutor 接入 EvidenceBundle 前”，避免在现有手写课程流程中加入检索、覆盖分支和来源等待后再次重复重构 | `docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md`、`docs/IMPLEMENTATION_PLAN_V044.md`、`docs/IMPLEMENTATION_PLAN_V1.md` |
| **新增** | 冻结窄范围主图：目标准备、诊断等待、资料覆盖、课程生成、质量修复和发布使用 LangGraph checkpoint/interrupt，节点内结构化模型调用使用 Pydantic AI；解析、切片、Embedding、检索、权限、评分与数据库事务仍是确定性服务 | `docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md`、`docs/AGENT_ROLES.md` |
| **修改** | 本条是当时的文档冻结记录；Python workflow service、Graph、checkpointer、interrupt 和首个 Pydantic AI 检索规划节点现已由上方后续记录实现 | `services/workflow/`、`docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md` |

### 2026-09-01 · 资料驱动教学闭环实施方案（仅文档冻结）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 增加编码前实施子方案，冻结“目标绑定资料 → 本地 Agentic RAG Tool → 固定 EvidenceBundle → 来源课程 → 来源题目 → 固定快照评分 → 资料不足时联网补充”的依赖顺序 | `docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md` |
| **修改** | 明确知识依据、教学策略和题目组织是三个独立层次：资料库/联网来源提供知识证据，版本化策略约束教学结构，LLM 只在已教学证据范围内组织题目 | `docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md`、`docs/IMPLEMENTATION_PLAN_V044.md` |
| **暂未实现** | 本条记录方案冻结当时的状态；Schema、目标资料 API 与 RAG Tool 核心已由上方后续记录实现，课程接线、题目评分和联网搜索仍未完成 | `docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md` |

### 2026-09-01 · 文本 Embedding 与向量检索 MVP（已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 增加独立本地 Embedding 服务，固定支持 `BAAI/bge-m3` 与 `Qwen/Qwen3-Embedding-0.6B` 两个白名单别名；服务按需加载且同一时间只保留一个模型，自动使用 CUDA，返回归一化 1024 维 float32 向量及模型加载/分词/编码耗时 | `services/embedding/` |
| **新增** | 数据库升级到 Schema V10，新增 `embedding_profiles`、`embedding_runs`、`source_chunk_embeddings`、`pipeline_runs` 与 `pipeline_stage_metrics`；向量按 `chunk_id + profile_id` 隔离，模型 revision、输入模板版本、设备、精度、批大小、Token、冷/热状态和失败信息可追踪 | `lib/db/embedding-schema.ts`、`lib/db/embeddings.ts`、`lib/db/pipeline-metrics.ts` |
| **新增** | 增加资料级向量化与语义查询接口：`GET/POST /api/knowledge-sources/[id]/embeddings` 查询状态或批量生成，`POST /api/knowledge-sources/[id]/search` 生成 Query Embedding、执行用户/资料/active ChunkSet 过滤、余弦召回、Parent 去重展开并固定 RetrievalRun 快照 | `app/api/knowledge-sources/[id]/embeddings/route.ts`、`app/api/knowledge-sources/[id]/search/route.ts` |
| **新增** | 增加 `npm run embedding:service` 和 `npm run embedding:smoke`；`.env.example` 补齐服务地址、超时、批大小、模型路径和设备配置 | `scripts/start-embedding-service.ps1`、`scripts/embedding-smoke.mjs`、`.env.example` |
| **当前边界** | 当前向量召回采用 SQLite BLOB 上的精确余弦扫描，适合个人 Demo 与离线 A/B；目标级 RAG Tool 已在上方阶段接入，但尚未实现 ANN/vector extension、真正 BM25/向量融合、reranker、固定检索评测集或资料库前端向量化控件 | `lib/db/embeddings.ts`、`lib/db/retrieval.ts`、`docs/RAG.MD` |

真实验收使用现有“Agent深入浅出”资料的同一 active ChunkSet（1,019 个 Child）、相同 `batch_size=8`、RTX 4070 Laptop GPU 和 float16 推理：BGE-M3 首次完整向量化约 16.9 秒，其中模型加载 4.06 秒、编码 10.86 秒；Qwen3-Embedding-0.6B 约 30.6 秒，其中模型加载 1.88 秒、编码 26.32 秒。两者均写入 1,019 条 1024 维向量，L2 范数为 1.0；同一查询均成功召回 Agent、工具调用与强化学习相关章节。该结果只证明真实链路与计时落库可用，不代表已经得出检索质量优劣结论。

### 2026-09-01 · RAG 全阶段计时契约（Embedding/检索已落地）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 冻结 `PipelineRun + StageMetric` 计时模型，覆盖 MinerU 提交/等待/下载、结构恢复、切片、FTS、模型加载、分词、Embedding 编码/持久化、BM25、向量召回、融合、重排、Parent 展开和 EvidenceBundle；同时记录模型 revision、设备、批量大小、Token/条数和缓存状态 | `docs/RAG.MD` |
| **修改** | 明确模型冷启动、队列等待、网络等待和实际计算必须分开；并行阶段不能简单相加，失败/重试也要保留实际耗时；BGE/Qwen 对比必须使用相同输入和参数，并报告中位数与 P95 | `docs/RAG.MD` |
| **修改** | Schema V10 已落地统一运行/阶段表，Embedding 与向量检索已记录 trace、总耗时、模型加载、分词、编码、持久化、Query Encoding、过滤、扫描、Parent 展开和证据快照；MinerU/切片/FTS 阶段埋点与性能查看界面仍待接入 | `lib/db/embedding-schema.ts`、`lib/db/pipeline-metrics.ts`、`docs/RAG.MD` |

### 2026-09-01 · Embedding 候选模型（本机已准备并接入）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 在 `G:\Embedding\bge-m3` 下载并固定 `BAAI/bge-m3` revision `5617a9f61b028005a4858fdac845db406aefb181`，只保留当前 PyTorch/Transformers 路线需要的模型文件 | 本机独立模型目录，不纳入仓库 |
| **新增** | 在 `G:\Embedding\qwen3-embedding-0.6b` 下载并固定 `Qwen/Qwen3-Embedding-0.6B` revision `97b0c614be4d77ee51c0cef4e5f07c00f9eb65b3` | 本机独立模型目录，不纳入仓库 |
| **修改** | 两套模型现已接入 Schema V10、批量向量化服务与资料级精确向量检索，可用于后续 A/B；课程 RAG 调用、混合检索和正式质量评测仍未完成 | `services/embedding/`、`lib/db/embeddings.ts`、`docs/RAG.MD` |

验证结果：BGE-M3 模型目录约 2.14GB，Qwen3 Embedding 目录约 1.13GB；两者均使用本地文件通过 Transformers 4.57.6 完成单条中文输入前向计算，输出 `(1, 1024)` 的有限值归一化向量，L2 范数为 1.0。首次误触发的 BGE ONNX 重复格式与不完整下载缓存约 542MB 已从两个精确目录清理，不影响已保留的 PyTorch 权重。

### 2026-09-01 · RAG 图片理解方案（仅文档冻结）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 对齐 RAG 文档与当前环境：本机 MinerU 3.4.5 GPU 服务和真实 PDF 链路已经跑通，不再沿用“MinerU 尚未部署”的旧描述 | `docs/RAG.MD` |
| **新增** | 冻结图片按需理解流程：先过滤 Logo、装饰图及已有充分图注的图片；流程图、架构图等默认以 `detail=low` 调用独立 VLM，仅在文字缺失、置信度低或需要高分辨率时升级 `original`；视觉摘要作为可追溯衍生产物，不能替代原图和原文 | `docs/RAG.MD` |
| **暂未实现** | 当前没有调用视觉模型、保存 `image_understanding` Artifact、图片摘要检索或原始图片向量化；这些工作排在文本 Embedding 与检索主链之后 | `docs/RAG.MD` |

### 2026-09-01 · MinerU 本机 GPU 加速（运行环境已切换）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 将本机外置 `G:\MinerU\.venv` 从 CPU 构建 `torch 2.8.0 / torchvision 0.23.0` 替换为 CUDA 12.8 构建 `torch 2.8.0+cu128 / torchvision 0.23.0+cu128`；MinerU 3.4.5、Python 3.12 和项目依赖版本保持不变 | 本机 MinerU 独立运行环境，不纳入仓库依赖锁 |
| **暂未实现** | 仓库仍不会自动安装 CUDA、PyTorch GPU wheel 或 MinerU 模型；其他机器部署时需要根据 NVIDIA 驱动、显存和操作系统单独配置，CPU 环境仍可运行但解析更慢 | `services/rag_ingestion/README.md` |

验证结果：`torch.cuda.is_available()=True`，设备为 `NVIDIA GeForce RTX 4070 Laptop GPU`，CUDA 12.8 张量计算成功，148 个 Python 包依赖检查全部兼容；外置 MinerU 6000 与 ingestion 8010 健康检查通过。同一份 66 页、8.29MB PDF 经完整 `MinerU → content_list_v2 → LlamaIndex Parent/Child` 链路返回 HTTP 200，GPU 首次对照耗时 40.7 秒，CPU 环境此前为 100–135 秒，约提速 2.5–3.3 倍。该数据是单机单样本工程基准，不代表所有 PDF 的固定性能。

### 2026-08-31 · V0.4.4 成熟解析与分块适配层（代码与契约已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 增加独立 Python ingestion 服务：按 MinerU 官方 protocol v2 执行健康检查、异步任务提交、状态轮询和安全 ZIP 读取，优先消费 Markdown 与 `content_list_v2.json`；页眉、页脚和页码不进入正文节点，标题、段落、列表、代码、表格、公式、图片说明保留为 Schema V9 `DocumentNode` | `services/rag_ingestion/growth_loop_rag/mineru_client.py`、`mineru_content.py`、`main.py` |
| **新增** | 使用 LlamaIndex 0.14.24 `HierarchicalNodeParser` 生成两级 Parent/Child，默认配置 2048/512 Token、20 Token overlap；参数完整写入不可变 ChunkSet，明确只是初始配置，后续必须用固定检索集调优 | `services/rag_ingestion/growth_loop_rag/chunking.py`、`settings.py`、`pyproject.toml`、`uv.lock` |
| **修改** | 上传与“优化切片”统一经过 `ingestKnowledgeSource`：配置 `RAG_INGESTION_URL` 时走 Python 服务，未配置或非强制模式失败时回退现有 TypeScript 解析；`RAG_INGESTION_REQUIRED=true` 可禁止静默回退 | `lib/knowledge/ingestion.ts`、`app/api/knowledge-sources/route.ts`、`app/api/knowledge-sources/[id]/rechunk/route.ts`、`lib/db/knowledge-sources.ts` |
| **新增** | 服务端返回值按现有 `ExtractedDocument + SourceChunkSetDraft` 校验后再落库，新增父子位置、包含关系、字符范围和 Token 合法性门禁；不改变 Schema V9，也不覆盖旧 ParseRun/ChunkSet | `lib/knowledge/ingestion.ts`、`lib/db/knowledge-sources.ts` |
| **新增** | 增加 MinerU 结构映射、安全 ZIP、protocol v2 异步任务和 LlamaIndex 父子完整性测试，并完成 Python 服务 → Next.js 适配器的真实 HTTP 文本联调 | `services/rag_ingestion/tests/` |
| **修复** | 真实 MinerU 3.4.5 联调后，将 `MINERU_EFFORT` 默认值从接口不接受的 `auto` 修正为 `medium`；MinerU 的健康检查、任务提交、状态查询和结果下载错误会携带 HTTP 状态与官方 `detail` 返回，不再统一退化成无原因的 500 | `services/rag_ingestion/growth_loop_rag/settings.py`、`mineru_client.py`、`.env.example` |
| **修改** | `.env.example` 增加 ingestion、MinerU 和分块参数；ESLint/Git 忽略 Python 虚拟环境与缓存；资料查看器可识别 LlamaIndex 策略 | `.env.example`、`.gitignore`、`eslint.config.mjs`、`app/source-library.tsx` |
| **暂未实现** | 仓库不会自动安装或分发 MinerU 模型，部署时仍需另行维护官方 `mineru-api` 并配置 `MINERU_API_URL`；本行记录成熟解析阶段当时的状态，Embedding 与目标级 RAG Tool 已由上方后续阶段实现，OCR 专项样本和正式检索评测仍未完成 | `services/rag_ingestion/README.md`、`docs/RAG.MD` |

验证结果：Python 3.12 环境下 6 项单测通过；`/health`、TXT `/v1/ingest` 与 Next.js `ingestKnowledgeSource` HTTP 联调通过，返回 `llamaindex-hierarchical-parent-child`；外置 MinerU 3.4.5 已用 1 页小 PDF 和 66 页、8.29MB 的真实 PDF 走通完整 `MinerU → Markdown/content_list_v2 → LlamaIndex Parent/Child` 链路，后者约耗时 135 秒，任务最终状态为 completed；`typecheck`、`lint`、Schema V9/FTS5、旧 TypeScript 切片回归和 Next.js 生产构建全部通过。此次结果证明解析适配器和数据契约可运行，不代表检索精度已经提高。

### 2026-08-31 · V0.4.4 结构化父子切片（阶段 B 已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 数据库升级到 Schema V9，增加 `document_parse_runs`、`document_nodes`、不可变 `source_chunk_sets` 和 `source_parent_chunks`；Child 切片通过 `chunk_set_id / parent_chunk_id` 形成正式父子关系，资料只用 `active_chunk_set_id` 指向未来检索使用的集合 | `lib/db/knowledge-schema.ts`、`lib/db/schema.ts`、`lib/db/index.ts` |
| **修改** | TXT、粘贴文本与 DOCX 先转为规范化 Markdown 和原子节点，再执行“结构边界 + 超长节点递归回退 + 父子分块”；标题、段落、列表、代码块、表格和公式不再直接混入固定字符窗口，默认 Child 约 220–700 Token、Parent 最多约 2400 Token且不做人为重叠 | `lib/knowledge/structure.ts`、`lib/knowledge/extract.ts`、`lib/knowledge/chunking.ts` |
| **修复** | `structure-parent-child-v2` 修复 PDF 视觉换行导致的“模\n型”断词，递归分割改为优先选择完整句末，并把“实验/示例/案例/练习/任务”等编号行识别为章节边界；页首尾纯数字页码和脚注不再混入普通正文 | `lib/knowledge/structure.ts`、`lib/knowledge/chunking.ts`、`scripts/validate-chunking.mjs` |
| **修改** | DOCX 从纯文本抽取升级为 Mammoth HTML → 规范化 Markdown，保留标题、段落、列表、代码和表格文字；PDF 仍使用 `pdf-parse` 保留逐页文本并做启发式结构识别，复杂版面与图片结构等待 MinerU 适配器 | `lib/knowledge/extract.ts`、`lib/knowledge/structure.ts` |
| **新增** | 每次重新解析/切片生成新的不可变 ParseRun 与 ChunkSet，全部成功后才原子切换 active 集合；资料查看器增加“优化切片”、父/子片段切换、章节路径、边界原因和父子 Token 信息 | `lib/db/knowledge-sources.ts`、`app/api/knowledge-sources/[id]/rechunk/route.ts`、`app/source-library.tsx`、`app/globals.css` |
| **修复** | V8 → V9 采用显式表重建迁移：为旧资料生成 legacy ParseRun/ChunkSet/Parent，保留原 Child ID 和已有证据外键，不清空用户资料；迁移前已保存 SQLite 主库、WAL 与 SHM 备份 | `lib/db/knowledge-schema.ts`、`scripts/migrate-database.mjs`、`data/backups/pre-schema-v9-20260831/` |
| **新增** | 增加结构切片回归与 V8 → V9 迁移回归，覆盖原子节点类型、父子完整性、超长节点递归回退、旧切片 ID 保留、FTS5 重建、外键与迁移幂等性 | `scripts/validate-chunking.mjs`、`scripts/validate-database-schema.mjs` |
| **暂未实现** | 本行是阶段 B 当时状态；MinerU/LlamaIndex 适配代码现已在上方阶段 C 完成，但 MinerU 模型部署、LLM 模糊边界辅助、Embedding/混合检索、目标资料绑定、Agentic RAG Tool、URL 导入和联网搜索仍未接入 | `docs/RAG.MD`、`docs/IMPLEMENTATION_PLAN_V044.md` |

验证结果：实际数据库已迁移到 Schema V9，`integrity_check=ok`，现有 1 份资料的 528 个旧 Child 全部保留并挂接到 528 个 legacy Parent；用 `structure-parent-child-v2` 对当前 314 页 PDF 只读重解析得到 1926 个 DocumentNode、599 个 Parent 和 936 个 Child，截图对应内容已从完整句子开始和结束，以“型之外”开头的 Child 为 0。`typecheck`、`lint`、Schema/FTS5 校验、结构切片回归和 Next.js 生产构建全部通过；该演练证明目标缺陷已修复，不代表所有资料的检索精度已经提升。

### 2026-08-30 · V0.4.4 个人资料库底座（阶段 A 已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 计划页增加“个人资料库”窗口，支持粘贴文本和上传 TXT/PDF/DOCX；展示资料类型、大小、字符数、片段数以及 `ready / ocr_required / failed` 真实状态，并提供二次确认软删除 | `app/source-library.tsx`、`app/dashboard-client.tsx`、`app/globals.css` |
| **新增** | 新增登录用户隔离的资料 API：`GET/POST /api/knowledge-sources` 与 `DELETE /api/knowledge-sources/[id]`；当前不设置固定文件大小/抽取字符上限，并阻止当前用户重复上传相同内容 | `app/api/knowledge-sources/`、`lib/db/knowledge-sources.ts` |
| **修改** | 按当前 Demo 需求移除 10 MB 原文件、200 万抽取字符和 2,000 chunks 的硬上限；DOCX 仍校验 Office 结构、异常压缩比与异常条目数量，避免压缩炸弹 | `lib/knowledge/extract.ts`、`lib/knowledge/chunking.ts`、`app/source-library.tsx` |
| **新增** | 上传文件或粘贴文本时可填写“资料说明”，引导用户描述资料内容、学习目的和重点范围；说明随资料元数据持久化，并预留给后续查询改写、资料筛选和教学上下文组装 | `app/source-library.tsx`、`app/api/knowledge-sources/route.ts`、`lib/db/knowledge-sources.ts`、`lib/db/schema.ts` |
| **新增** | TXT 支持 UTF-8/GB18030，PDF 使用逐页纯文本提取并识别扫描件，DOCX 只提取纯文本；原文件 BLOB、抽取文本、版本、哈希、页码和确定性分块统一保存在 SQLite | `lib/knowledge/extract.ts`、`lib/knowledge/chunking.ts` |
| **新增** | 已解析资料增加“查看切片”：采用分页接口每次最多读取 50 个片段，支持按标题/正文搜索、高亮命中、片段目录、页码/字符范围/Token 估算、按钮与键盘前后切换；接口始终按当前登录用户和资料归属过滤 | `app/source-library.tsx`、`app/api/knowledge-sources/[id]/chunks/route.ts`、`lib/db/knowledge-sources.ts`、`app/globals.css` |
| **新增** | 新增 RAG 总体架构文档，统一粘贴/上传/用户 URL/联网搜索的数据链，冻结 `Source → Version → ParseRun → DocumentNode → ChunkSet → Parent/Child → RetrievalRun → 课程/题目/评分` 关系；明确 URL 只抓取一次、不自动更新，搜索候选不直接入库，LLM 只决定原子单元边界且不得改写证据 | `docs/RAG.MD` |
| **新增** | 数据库升级到 Schema V8，增加资料、版本、片段、FTS5 索引/触发器、目标关系、检索运行快照和题目来源表；数据库校验新增 FTS5 自动索引命中断言 | `lib/db/schema.ts`、`scripts/validate-database-schema.mjs` |
| **修改** | 明确 RAG 不是独立 Agent；未来封装为 `search_knowledge_base`、`read_source_chunks`、`check_source_coverage` 等可审计 Tool，由 Tutor/Planner 决定何时调用 | `docs/IMPLEMENTATION_PLAN_V044.md` |
| **暂未实现** | 当前只完成个人资料库底座，尚未实现目标资料绑定、Agent Tool 注册、真实 FTS 查询/Token 预算、课程与题目 chunk 引用、引用抽屉或固定来源评分；也未加入 OCR、Embedding、URL 和联网搜索 | `docs/IMPLEMENTATION_PLAN_V044.md` |

验证结果：`typecheck`、`lint`、Schema V8/FTS5 数据库校验和 Next.js 生产构建通过；隔离数据库端到端验证覆盖粘贴文本、TXT/PDF/DOCX 上传、列表和软删除，以及切片分页、关键词搜索和跨用户读取返回 404，临时账号、Cookie 与数据库均已清理。上述结果只证明资料库底座可运行，不代表 Agentic RAG 或可信来源教学闭环已经完成。

### 2026-08-30 · V0.4.3 通用课程内容引擎（工程 MVP 已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 课程不再固定为 5 节，由目标期限、每周投入和能力数量动态生成 3–12 节路线；能力点增加概念理解、程序技能、问题解决、表达沟通、记忆辨析和综合创作六类教学策略 | `lib/agents/planner.ts`、`lib/agents/types.ts` |
| **新增** | 新课正文改为版本化 `LearningBlock[]`，统一保存讲解/关系/对比、完整示例/演示/代码实验、错误/边界和引导/回忆/表达练习，并为每节定义可观察的完成证据 | `lib/learning-program/types.ts`、`lib/agents/tutor.ts` |
| **新增** | 发布前执行确定性质量门禁和独立语义复核；未通过时最多定向修复 2 次，仍失败则保存 `quality_failed` 并在课程页提供重试，不再把通用回退伪装成正式 AI 课程 | `lib/learning-program/quality.ts`、`lib/learning-loop/service.ts`、`app/learning-studio.tsx` |
| **新增** | 形成性题目只在正文通过质量门禁后生成，每题绑定不可变内容版本、已教学块、证据类型和预期概念；评分尝试同时保存题目快照，公开接口继续剥离参考答案和 rubric | `lib/agents/tutor.ts`、`lib/db/programs.ts`、`app/api/learning-program/route.ts` |
| **新增** | 数据库升级到 V6，新增 `lesson_content_versions`、`lesson_quality_reports`、`lesson_block_sources`，并为能力、课节和评测补充内容版本、来源、质量、能力类型和题目快照字段 | `lib/db/schema.ts`、`scripts/validate-database-schema.mjs` |
| **修改** | 课程页按教学块渲染，展示能力类型、来源状态和质量状态；失败课节不展示正式答题入口，可通过“重新生成本节”再次进入质量链 | `app/learning-studio.tsx`、`app/globals.css`、`app/api/learning-program/route.ts` |
| **修复** | 模型节点支持 75–120 秒的分级等待并把超时、空响应、解析和校验失败写入 `agent_runs.error_message`；示例步骤单项上限从通用列表的 180 字符放宽到 2400 字符，避免完整代码被清洗层截断 | `lib/agents/shared.ts`、`lib/agents/planner.ts`、`lib/agents/tutor.ts` |
| **新增** | 新增 V0.4.3 质量回归，覆盖 Java 并发、技术演讲和近代史辨析三类目标、动态课时、教学块完整性、题目追溯、答案隔离、质量阶段和非代码能力策略 | `scripts/v043-quality-smoke.mjs`、`scripts/learning-program-smoke.mjs` |
| **新增** | V0.4.3.1 基础课件阅读器已落地：结构化新课一次只渲染一个教学块，支持页码/进度点、按钮与键盘翻页、交付物页、独立考核页和按内容版本恢复阅读位置；考核页关闭导师问答 | `app/learning-studio.tsx`、`app/globals.css` |
| **修改** | 对齐课件阅读器文档与当前实现，明确上下文块提问、困惑/收藏、交互块作答、长代码工具和 `open_book` 仍未实现；当前 Schema V7 已被学习作息占用，V8/V9 用于 V0.4.4 资料库与结构化切片，未来课堂标注迁移顺延为 V10 | `docs/IMPLEMENTATION_PLAN_V043.md` |

验证结果：`typecheck`、`lint`、`db:validate` 和隔离数据库规则模式 E2E 已通过；V0.4.3 核心引入 Schema V6，当前项目后续已升级到 Schema V7（25 张表、17 项增量字段、`integrity_check=ok`）。规则回归中 Java/演讲/历史三个固定样例均通过质量门禁，所有正式题目绑定当前内容版本和有效教学块，公开响应中的参考答案与 rubric 为 0。浏览器实测阅读器只渲染当前教学块，按钮和方向键可连续翻页，刷新后能从 `03 / 09` 恢复。真实模型完整课程链仍可能接近 5 分钟；当前不能据此宣称学习效果提升，也不能把尚未实现的课堂标注写成现有能力。

### 2026-08-28 · 逐题自适应初始诊断与考官评分进度（已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 初始诊断不再一次生成整套模板题，而是先生成一道带固定数据、代码、约束、故障现象或明确交付物的具体题；LLM 输出若仍包含“请给出一个具体场景”等空泛模板，会自动退回到本地具体题 | `lib/agents/examiner.ts`、`lib/learning-loop/service.ts` |
| **新增** | 每回答一题立即由 Examiner 独立评分，并维护各能力的“已通过最高难度 / 未通过最低难度”边界；高分优先上探、低分优先下探、中间分在相近难度复核，同一能力边界收敛后再切换能力 | `lib/learning-loop/adaptive.ts`、`lib/learning-loop/adaptive-diagnostic.ts` |
| **修改** | 一知半解采用最少 5、最多 10 题，小有所成采用最少 6、最多 12 题；达到最少题数且能力边界已收敛时提前结束，否则在题数上限处停止，避免固定题量既浪费 Token 又测不出上下限 | `lib/learning-loop/service.ts` |
| **新增** | `POST /api/diagnostics` 增加 `answer-stream` 逐题 NDJSON 协议，真实返回“读取证据 → 本题评分 → 更新边界 → 生成下一题 / 汇总基线 → 生成课程”进度；只展示工作流阶段和产物，不暴露模型内部思维链 | `app/api/diagnostics/route.ts`、`lib/learning-loop/adaptive-diagnostic.ts` |
| **修改** | 诊断弹窗始终只显示当前题；提交后叠加独立的居中考官评估窗口、动画、进度条和最近节点，完成后展示本题得分、反馈及难度上调/下调提示 | `app/dashboard-client.tsx`、`app/globals.css` |
| **新增** | 数据库升级到 V5：诊断记录保存最少/最多题数、已答数和自适应边界快照；`diagnostic_responses` 逐题保存答案、分数、反馈、评分模式与模型信息，刷新后可以从最后一道未答题继续 | `lib/db/schema.ts`、`lib/db/learning-loop.ts` |
| **修改** | 旧版尚未完成的整套诊断不会被误当作自适应诊断继续使用，重新开始时会过期旧记录并创建新的逐题诊断；已完成诊断仍保持幂等回放 | `lib/db/learning-loop.ts`、`lib/learning-loop/service.ts` |
| **新增** | 端到端回归同时注入高质量答案与“不会”答案，断言出现难度上调和下调、每轮只新增一题、逐题证据落库、评分进度到达 100%、诊断幂等回放及后续课程闭环 | `scripts/learning-program-smoke.mjs` |
| **修改** | `V0.4.2 实施方案` 增加面向其他 LLM 的当前实现基线，集中记录题目来源、自适应算法、NDJSON 协议、数据库 V5、UI 行为、角色边界、代码索引、验证结果、未实现项和后续修改约束，并明确标注下文早期固定题量方案为历史记录 | `docs/IMPLEMENTATION_PLAN_V042.md` |
| **修改** | 全面修订 V1 路线图：将教学质量提升为正式考核的前置门禁，增加通用能力类型、结构化 `LearningBlock`、课程质量报告、私有资料/联网 RAG 来源链和可见回退；本行记录 8 月 28 日当时“V0.4.5 再迁移框架”的顺序，已被上方 9 月 1 日的提前接入决策取代 | `docs/IMPLEMENTATION_PLAN_V1.md` |
| **新增** | 编写独立 V0.4.3 实施方案，冻结“结构化课程生成 → 双层质量门禁 → 定向修复 → 教学块对齐小测 → 版本化证据”的范围，补充通用能力模型、拟定数据库 V6、测试指标与 Definition of Done；本行记录的是 8 月 28 日当时状态，后续实现见上方 V0.4.3 记录 | `docs/IMPLEMENTATION_PLAN_V043.md` |
| **修改** | 对齐跨文档版本边界：V0.4.3 负责教学质量，V0.4.4 负责可信 RAG，Pydantic AI/LangGraph 框架迁移统一调整到 V0.4.5；同步修正 V0.4.1 交接、V0.4.2 交接与 Roles 说明 | `docs/HANDOFF_V041.md`、`docs/IMPLEMENTATION_PLAN_V042.md`、`docs/AGENT_ROLES.md`、`docs/IMPLEMENTATION_PLAN_V1.md` |
| **新增** | 增加邀请制公网 Demo 部署方案，固定支持 `node:sqlite` 的 Node.js 22/24 运行时，并记录 Zeabur GitHub 部署、`/data` 持久化卷、HTTPS Cookie、LLM Secret、上线验收和体验反馈边界 | `package.json`、`.node-version`、`docs/DEMO_DEPLOYMENT.md` |

验证结果：`typecheck`、`lint`、`db:validate`、Next.js 生产构建全部通过；隔离 SQLite 端到端回归返回 `ok: true`，自适应诊断在 10 题内完成，高低分分别触发上探和下探，最终生成 5 节课程骨架并完成首课评测闭环。新增 Demo 部署配置后再次通过 Next.js 生产构建。临时测试数据库已删除，未影响日常数据。

### 2026-08-27 · 长期目标删除闭环（已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 长期目标卡片增加低干扰的删除入口；点击后显示二次确认窗口，明确关联数据范围和不可撤销性，确认期间锁定重复操作 | `app/dashboard-client.tsx`、`app/globals.css` |
| **新增** | `DELETE /api/goals` 按当前登录用户校验目标归属，目标不存在或不属于当前用户时不执行删除 | `app/api/goals/route.ts` |
| **新增** | 删除事务先清理课程关联任务与目标 Agent 运行记录，再删除目标；能力、掌握度、诊断、课程、课节和评测由外键级联清理，普通学习记录保留 | `lib/db/goals.ts` |
| **修改** | 删除成功后前端同步移除目标和被删除的今日任务，并清理已删除课程的本地活动课程指针 | `app/dashboard-client.tsx` |
| **新增** | 端到端回归覆盖目标删除、课程任务清理、课程不可再读取以及其他目标不受影响 | `scripts/learning-program-smoke.mjs` |

验证结果：`typecheck`、`lint`、`db:validate`、Next.js 生产构建均通过；隔离 SQLite 端到端回归返回 `ok: true`、`deletedTaskCount: 1`。临时测试数据库已删除。

### 2026-08-27 · 居中单题诊断与独立进度窗口（已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修复** | 初始诊断不再沿用靠右展开的测验抽屉，改为视口居中的固定宽度弹窗，避免题目窗口偏斜和整页滚动 | `app/dashboard-client.tsx`、`app/globals.css` |
| **修改** | 诊断题采用渐进式单题作答：显示题号、总题数、已答数量和进度条，支持上一题、下一题与最终提交，回答内容在题间切换时保留 | `app/dashboard-client.tsx` |
| **修改** | 创建目标提交后不再从表单下方衍生进度区域，而是将表单切换为独立的小型进度窗口，集中展示当前节点、百分比和最近执行记录 | `app/dashboard-client.tsx`、`app/globals.css` |
| **新增** | 进度窗口增加轨道旋转、中心图标呼吸和节点光点动画；为 `prefers-reduced-motion` 用户自动关闭动画和过渡 | `app/globals.css` |

验证结果：`typecheck`、`lint`、Next.js 生产构建均通过；本轮只调整前端交互与样式，原有流式 API 和诊断提交协议未变。

### 2026-08-27 · 创建目标真实进度流（已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 为目标准备闭环增加真实阶段回调：读取档案、Planner 拆能力、Examiner 生成初始诊断、Planner 编排课程、Tutor 生成首课与巩固题、持久化，每个节点在实际调用前后报告进度 | `lib/learning-loop/service.ts` |
| **新增** | `POST /api/learning-program` 增加 `prepare-stream` 动作，以 NDJSON 流持续返回 `progress / result / error` 事件，并关闭代理缓冲 | `app/api/learning-program/route.ts` |
| **修改** | 创建目标弹窗增加百分比进度条、当前阶段、最近节点和失败详情；创建期间禁止误关弹窗。界面只展示可验证的工作流节点与产物进度，不暴露模型内部推理原文 | `app/dashboard-client.tsx`、`app/globals.css` |
| **新增** | 端到端冒烟测试覆盖流式诊断分支，并断言能力地图、诊断节点、单调递增百分比和最终 100% 结果 | `scripts/learning-program-smoke.mjs` |

验证结果：`typecheck`、`lint`、`db:validate`、Next.js 生产构建均通过；隔离 SQLite 端到端回归返回 `ok: true`，进度节点为 `load_goal → skill_map → diagnostic → persist`，百分比单调递增并以 `100%` 结束。临时测试数据库已删除。

### 2026-08-27 · 初始诊断可见性与多目标课程切换（已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修复** | 浏览器宽度小于 820px 时不再误进入尚未接入诊断弹层的旧移动壳；当前阶段浏览器统一使用桌面闭环，移动端继续暂停开发 | `app/dashboard-client.tsx` |
| **修复** | 一知半解/小有所成目标创建后，诊断弹层使用最高层级展示；目标卡片同步显示「待初始诊断 / 开始诊断」，关闭弹层后仍有明确恢复入口 | `app/dashboard-client.tsx`、`app/globals.css` |
| **修改** | 创建目标或准备学习路径失败时展示服务端具体错误，不再只显示“没有任何提示”的泛化结果 | `app/dashboard-client.tsx` |
| **新增** | 仪表盘为每个目标返回 `selfLevel`、`diagnosticStatus`、活动 `learningProgramId`；课程页增加学习目标切换栏，可在多门课程之间立即切换，也可从课程页恢复待完成诊断 | `lib/db/dashboard.ts`、`lib/demo-data.ts`、`app/learning-studio.tsx` |
| **修改** | 按用户要求对本地账号执行一次性学习目标重置：删除 4 个历史目标、3 个关联引导任务和 8 条关联 Agent 调用，并由外键级联清理课程、能力和诊断数据；其他账号和学习记录未动 | 本地 `data/growth-loop.sqlite`，不属于源码提交 |
| **新增** | 闭环测试增加多目标断言，确认同一账号下诊断目标与初学者目标都返回独立课程切换信息 | `scripts/learning-program-smoke.mjs` |

验证结果：`typecheck`、`lint`、Next.js 生产构建均通过；隔离 SQLite 端到端回归返回 `ok: true`、`switchableGoals: 2`。临时测试数据库已删除。

### 2026-08-27 · V0.4.2 诊断证据驱动最小闭环（已实现）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 创建目标必须选择「初学者 / 一知半解 / 小有所成」；初学者直接进入首课，后两档分别进入 5 / 8 题初始诊断 | `app/dashboard-client.tsx`、`app/api/goals/route.ts` |
| **新增** | Planner 只拆能力和课程骨架，Tutor 负责当前课正文、巩固题与形成性评分，Examiner 只接收目标能力与诊断蓝图，不读取日常学习背景 | `lib/agents/`、`lib/learning-loop/service.ts` |
| **新增** | 能力点与未知基线真实写入 `goal_skills` / `skill_mastery`；`confidence = 0` 表示尚无证据，不把占位 0 当作一次零分 | `lib/db/learning-loop.ts` |
| **新增** | 诊断题、固定答案/rubric 快照、答案、逐能力得分和基线结论持久化；重复准备复用活动诊断，重复提交已完成诊断回放同一结果 | `app/api/diagnostics/route.ts`、`lib/db/learning-loop.ts` |
| **修改** | 课程先保存完整 3–5 节路线，但只生成第一节正文与 3 道巩固题；其余章节为 `planned`，上一节通过后按最新掌握度和难度生成下一节 | `lib/learning-loop/service.ts`、`lib/db/programs.ts`、`app/learning-studio.tsx` |
| **修改** | 课后评分、掌握度更新、章节通过、课程任务完成、目标进度与下一课解锁放进同一个 SQLite 事务；普通任务 PATCH 遇到课程任务返回 409 | `lib/db/programs.ts`、`app/api/tasks/[id]/route.ts` |
| **新增** | 数据库升级到 V4，为课节增加 `generation_mode`、`generation_status`、`difficulty`，同时记录角色调用的 provider、model、Token 与耗时 | `lib/db/schema.ts`、`lib/db/learning-loop.ts` |
| **修改** | `LLM_PROVIDER=demo/rules/local` 明确关闭外部模型并使用本地规则回退，便于无 Key 运行和确定性回归 | `lib/agents/shared.ts`、`lib/learning-program/service.ts` |
| **新增** | 闭环冒烟测试覆盖：诊断门禁与幂等、初学者分支、首课按需生成、参考答案隔离、课程任务防绕过、评测证据更新、下一课生成 | `scripts/learning-program-smoke.mjs` |
| **暂未实现** | LangGraph checkpoint / interrupt、Pydantic AI Python 服务、导师不合格后的专门补课循环、阶段大考/模拟面试/毕业门禁 | V0.4.5–V0.5 |

验证结果：`typecheck`、`lint`、`db:validate`、Next.js 生产构建全部通过；在独立 SQLite 数据库上完成多次端到端回归，最终结果为 `ok: true`，诊断 100 分、首节形成性评测 85 分、尝试次数按 1 → 2 递增、关联任务自动完成、第二节难度上调并由 `planned` 转为 `ready`。临时数据库已删除，未污染日常数据。

### 2026-08-27 · V0.4.2 初始诊断前置（方案修订记录）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | V0.4.2 不再按“默认初学者、掌握度为 0、立即生成整套课程”推进；改为创建目标时必选三档基础，先拆能力，再决定是否进入诊断 | `docs/IMPLEMENTATION_PLAN_V042.md` |
| **修改** | `familiar`、`intermediate` 必须完成 5–8 道初始诊断题，诊断完成是课程生成门禁；`beginner` 可跳过，但 `confidence = 0` 只表示尚无证据 | `docs/IMPLEMENTATION_PLAN_V042.md` |
| **修改** | 课程在基线确定后只生成完整骨架与第一节内容，后续章节在上一节产生评测证据后按需生成，使难度调整真正影响未来题目 | `docs/IMPLEMENTATION_PLAN_V042.md` |
| **修改** | V0.4.2 使用数据库状态和幂等 API 实现诊断暂停/恢复；本行记录 8 月 27 日当时“V0.4.5 再迁移”的路线，已被上方 9 月 1 日的 V0.4.4 最小课程主图决策取代 | `docs/IMPLEMENTATION_PLAN_V042.md` |
| **修改** | 明确评分证据、章节通过和关联任务完成必须由 Next.js 在同一事务中处理，课程任务不能通过普通 PATCH 绕过评测门禁 | `docs/IMPLEMENTATION_PLAN_V042.md` |
| **修改** | 同步收紧 Roles 与 V1 契约：盲评不再宣称绝对确定性；角色共享 policy、不同操作使用独立 prompt；题目绑定 `skillId` 和 `maxScore`；权威学习状态在 Next.js 事务内计算 | `docs/AGENT_ROLES.md`、`docs/IMPLEMENTATION_PLAN_V1.md` |
| **修改** | 重新划分考核职责：导师根据实际教学内容负责课内巩固题、课后小测和补充讲解；考官/面试官只负责初始诊断、阶段大考、模拟面试与毕业门禁，两类证据分开解释 | `docs/IMPLEMENTATION_PLAN_V042.md`、`docs/AGENT_ROLES.md`、`docs/IMPLEMENTATION_PLAN_V1.md` |
| **修改** | 本节保留为当时的方案修订记录；实际落地情况以上方「V0.4.2 诊断证据驱动最小闭环」为准 | 当前实现 |

### 2026-08-27 · 目标周期可编辑与数据库 V3（未发布）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 创建目标的「大致周期」和「每周投入」由写死的下拉改为日期选择器和 1–40 小时输入框 | `app/dashboard-client.tsx` |
| **新增** | `goals.target_date` 保存可计算的目标日期（可为 NULL 表示不设期限）；`horizon` 改为由它在服务端派生，不再由客户端提交 | `lib/db/schema.ts`、`lib/goal-schedule.ts`、`app/api/goals/route.ts` |
| **新增** | 幂等加列机制 `COLUMN_ADDITIONS`：新库走建表语句、老库走 `ALTER TABLE`，补上 `CREATE TABLE IF NOT EXISTS` 覆盖不到的新列 | `lib/db/index.ts`、`lib/db/schema.ts` |
| **修改** | 数据库版本由 2 升级到 3；`db:validate` 增加两项校验——每个待补列必须已写进建表语句，以及在模拟老库上验证 ALTER 生效且重复执行会被拒绝 | `scripts/validate-database-schema.mjs` |
| **新增** | 创建目标弹窗显示「还剩 N 周 · 约 N 小时可投入」，由代码按剩余周数 × 每周投入算出 | `lib/goal-schedule.ts`、`app/globals.css` |
| **修改** | 填了目标日期但不是将来的合法日期时返回 400，不再静默丢弃 | `app/api/goals/route.ts` |
| **暂未实现** | 规划师 Agent 的可行性审查（`estimatedHours`、`verdict`、一键采纳建议）与后续进度监督 | V0.4.2，依赖 `goal_skills` 能力拆解先落地 |

### 2026-08-27 · 项目亮点与量化账本（未发布）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 建立持续更新的项目亮点文档，区分当前可证明成果、未来能力、关键风险和简历表述 | `项目亮点.MD` |
| **新增** | 定义有效完成率、学习增益、规划命中率、结构化输出成功率、工作流恢复率、Token 成本和 P95 延迟等量化口径 | `项目亮点.MD` |

### 2026-08-26 · V0.4.1 课程数据持久化（未发布）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 课程正文的唯一来源改为服务端数据库；浏览器只缓存当前 `programId`，清缓存或换设备后课程与进度仍可恢复 | `lib/db/programs.ts`、`app/learning-studio.tsx` |
| **修改** | `/api/learning-program` 的 `tutor` 与 `grade` 改为 `programId + lessonId` 入参，请求体不再携带课程正文，移除 180KB 上限 | `app/api/learning-program/route.ts` |
| **修复** | 参考答案与评分 rubric 不再下发到客户端，评分只读取服务端持有的固定快照 | `lib/db/programs.ts`、`lib/learning-program/types.ts` |
| **新增** | 每节课后题增加 `referenceAnswer`，与 rubric 一起随课程落库 | `lib/learning-program/service.ts` |
| **修改** | `POST /api/goals` 增收 `selfLevel`、`weeklyHours` 和 `background`，与 `goals` 同事务写入 | `lib/db/goals.ts`、`app/api/goals/route.ts` |
| **修改** | `generate` 只接收 `goalId`，主题与每周投入从目标和自评档案读取，落库后返回 `programId` | `app/api/learning-program/route.ts` |
| **修改** | 课程任务改用 `task_lesson_links` 关联，替换原先的按标题匹配和 `course-` 前缀 id | `app/api/tasks/route.ts`、`lib/db/dashboard.ts` |
| **新增** | 课后评分写入 `lesson_assessment_attempts`，重测让 `attempt_number` 递增，不覆盖历史尝试；合格后章节状态落库为 `passed` | `lib/db/programs.ts` |
| **新增** | `GET /api/learning-program?program=current|<programId>` 读取登录用户的课程正文 | `app/api/learning-program/route.ts` |
| **暂未实现** | 三档自评控件、诊断分支和 `goal_skills` / `skill_mastery` 写入 | V0.4.2 |

实施顺序与完成标准见 [V0.4.1 交接说明](docs/HANDOFF_V041.md)。

### 2026-08-26 · 学习闭环数据库 V2（未发布）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 目标自评档案：初学者、一知半解、小有所成，以及诊断状态和基线结果 | `goal_learning_profiles` |
| **新增** | 能力拆解和用户掌握度，支持后续按能力权重计算目标进度 | `goal_skills`、`skill_mastery` |
| **新增** | 课程、模块、课节及正式任务课程关联，准备替换 localStorage 和标题匹配 | `learning_programs`、`course_modules`、`course_lessons`、`task_lesson_links` |
| **新增** | LLM 诊断题快照、诊断尝试和课后考核历史，保留每次答案与评分 | `diagnostic_*`、`lesson_assessment_attempts` |
| **新增** | LangGraph 工作流索引和 Agent 调用记录，包括模型、Token、耗时与错误 | `workflow_runs`、`agent_runs` |
| **新增** | 内存 SQLite schema 校验命令 `npm.cmd run db:validate` | `scripts/validate-database-schema.mjs` |
| **修改** | 数据库版本由 1 升级到 2；现有 SQLite 启动时使用 `CREATE TABLE IF NOT EXISTS` 增量建表 | `lib/db/schema.ts` |
| **暂未实现** | 新表已创建，但创建目标 API、自评页面、诊断工作流和 Python Agent 服务尚未接入 | V0.4.1–V0.4.3 |

题库生成、LangGraph 分支、Pydantic AI 节点契约和分阶段实施顺序见 [V1 实施方案](docs/IMPLEMENTATION_PLAN_V1.md)。

### 2026-08-26 · 桌面端信息架构与学习闭环（未发布）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 桌面端统一为“导航栏 + 主内容”的两栏结构，移除所有页面右侧信息栏 | `app/dashboard-client.tsx`、`app/globals.css` |
| **修改** | 保留原项目 `Manrope`、`Noto Serif SC`、`DM Mono` 字体栈及既有字号尺度；本轮只调整结构和交互 | `app/globals.css` |
| **修改** | 计划、课程、记录、成长页移除与顶部大标题重复的页内标题，只保留一层页面标题 | `app/dashboard-client.tsx`、`app/learning-studio.tsx` |
| **新增** | 今日页按 05:00–18:59 / 19:00–04:59 自动展示白天或晚上内容，同时允许手动切换 | `app/dashboard-client.tsx` |
| **新增** | “随手一记”改为按钮和弹窗输入，不再长期占据今日页 | `app/dashboard-client.tsx`、`app/globals.css` |
| **修改** | 今日任务点击后进入课程；课程答题达到 60 分才自动完成关联任务，结果分为不合格、合格、良、优 | `app/dashboard-client.tsx`、`app/learning-studio.tsx` |
| **新增** | 从课程加入今日计划的任务写入数据库，刷新后仍保留 | `POST /api/tasks` |
| **修改** | 计划页只保留互斥的“本周任务 / 长期任务”视图，并用时间线展示任务路径 | `app/dashboard-client.tsx`、`app/globals.css` |
| **新增** | 计划页可以创建长期目标并写入数据库 | `POST /api/goals` |
| **修改** | 课程目标表单合并到“创建目标”：同时收集完成标准、当前基础和每周投入，创建目标后直接生成并进入课程 | `app/dashboard-client.tsx`、`app/learning-studio.tsx` |
| **修改** | 记录页默认展开今天、折叠更早记录；成长页移除单纯等级指标，改为行动、投入和掌握证据 | `app/dashboard-client.tsx` |
| **暂未实现** | 课程内容和评分仍保存在当前浏览器 localStorage；数据库任务与课程的正式关系表、多端同步以后补充 | 后续课程持久化版本 |

本次验证通过 `typecheck`、`lint` 和生产构建；另在隔离 SQLite 数据库中完成桌面端页面回归，确认两栏布局、原字体/字号、昼夜切换、随手一记弹窗、计划切换、创建目标弹窗和任务跳转课程均正常。隔离测试数据已删除。

### 2026-08-26 · 数据库与账号系统（未发布）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **新增** | 使用 username 和密码注册、登录、退出，不再依赖邮箱 | `app/login/`、`app/api/auth/` |
| **新增** | 密码属于 User，但仅保存经过 scrypt 处理的 `password_hash`，不保存明文 | `lib/auth/password.ts`、`lib/db/schema.ts` |
| **新增** | 数据库 Session 和 HttpOnly Cookie；默认会话有效期为 30 天 | `lib/auth/session.ts`、`sessions` 表 |
| **新增** | SQLite 用户数据层及迁移机制 | `lib/db/`、`data/growth-loop.sqlite` |
| **新增** | 首批表：`users`、`sessions`、`goals`、`tasks`、`activity_logs`、`ledger_entries` | `lib/db/schema.ts` |
| **修改** | 首页改为受保护页面；未登录访问 `/` 会跳转到 `/login` | `app/page.tsx` |
| **修改** | 工作台从数据库读取当前用户、目标、任务、记录、积分和近七天统计 | `/api/dashboard`、`lib/db/dashboard.ts` |
| **修改** | 任务完成状态、学习记录、XP、Coin 和连续有效行动天数刷新后仍会保留 | `/api/tasks/[id]`、`/api/activity-logs` |
| **修改** | 桌面端与移动端均提供退出登录入口 | `app/dashboard-client.tsx`、`app/mobile-shell.tsx` |
| **修改** | `.env.example` 增加 SQLite 路径和本地 HTTP Cookie 配置；数据库文件不提交 Git | `.env.example`、`.gitignore` |
| **暂未实现** | LLM 可以提出目标进度，但“服务端校验后写入 `goals.progress_percent`”尚未接入 | 后续目标进度接口 |

本次端到端验证覆盖：创建账号、账号密码登录、退出、未登录访问保护、任务状态持久化、学习记录持久化、积分流水和连续行动天数更新。验收使用的临时账号及数据已删除。

### 2026-08-26 · fork 功能兼容迁移（未发布）

| 标记 | 改动 | 主要位置 |
|---|---|---|
| **修改** | 将数据库与账号功能迁入个人 fork，同时保留 fork 已有的 AI 学习程序 | `app/dashboard-client.tsx`、`app/mobile-shell.tsx` |
| **修改** | 登录后的桌面端和移动端继续提供“课程”入口，课程可加入今日计划 | `app/learning-studio.tsx`、`/api/learning-program` |
| **修复** | 避免直接覆盖 fork 中已经修改的 README、页面、移动端外壳和样式 | 三方合并迁移 |
| **修复** | 排除 IntelliJ IDEA 的本机项目配置，避免误提交 `.idea` | `.gitignore` |

## 现在能做什么

| 能力 | 当前实现 | 入口 |
|---|---|---|
| 本地账号 | username 注册/登录、密码哈希、数据库会话、用户数据隔离 | `/login`、`/api/auth/*` |
| 今日昼夜工作台 | 白天看今日任务和随手一记，晚上看完成情况并进行三问收束 | 首页 |
| 学习闭环 | 目标能力拆解 → 可选初始诊断 → 首课 → 导师形成性评测 → 原子更新掌握度/任务 → 按需生成下一课 | `/api/diagnostics`、`/api/learning-program`、课程页 |
| AI 学习程序 | 保存完整路线但逐节生成内容；每节包含导师讲解、练习、交付物和 3 道巩固题，合格后才解锁并生成下一节 | `/api/learning-program`、`POST /api/tasks`、课程页 |
| 目标与计划 | 创建长期目标时必选三档基础；未完成诊断可从目标卡片继续；支持本周/长期视图、任务时间线和目标拆解 | `/api/goals`、`/api/learning-program`、计划页 |
| 个人资料库 | 粘贴文本或上传 TXT/PDF/DOCX，服务端提取纯文本、确定性分块并建立 FTS5 索引；扫描 PDF 会明确提示需要 OCR | `/api/knowledge-sources`、计划页“个人资料库” |
| 成长记录 | 学习记录、连续有效行动、XP 与 Coin 流水持久化 | `/api/activity-logs`、记录页/成长页 |
| 晚间回顾 | 统一总结当天记录，并依次追问最重要行动、真正理解和明日一步 | 首页晚报入口、Agent `review` 意图 |
| 微信入口 | 微信公众号首次验证、明文 XML 文本回调、签名校验、LLM 超时回退 | `/api/wechat` |
| Android App | 独立移动壳 v4；首页一屏 AI 会面，不堆功能、不产生首页纵向滚动 | `android/`、APK |
| 可复现调试 | doctor、build、install、run、smoke、logs；面向人和 AI | `scripts/debug-apk.ps1` |

## 产品形态

今日页按时间自动收纳内容。白天只显示今天要做的事和“随手一记”按钮；点击任务进入对应课程。晚上显示同款完成情况卡片和三问每日收束，不再同时堆叠白天与晚上的全部模块。

桌面端使用“导航栏 + 主内容”两栏结构，计划、课程、记录、成长保持独立入口，并且每页只保留顶部的一层大标题。计划页把本周任务和长期任务分开显示，创建目标时一并收集课程编排信息；课程页只负责学习和评测，不再重复创建目标。记录页默认展开今天并折叠更早记录；成长页只展示可验证的行动、投入和掌握证据。

个人资料库已经形成上传、解析、版本、分块、Embedding、目标范围和可审计检索底座。启用 workflow service 后，课程生成前会建立 RetrievalRun 并处理资料不足分支；但 EvidenceBundle 尚未传入 Tutor 的教学内容 Prompt，也没有块级引用门禁，因此现有课程仍不能标记为 `grounded`。RAG 保持为 Agent 可调用 Tool，不新增“RAG Agent”。

当用户有一个明确学习目标时，可从“课程”工作台（手机端位于“路线”的二级入口）进入 AI 学习程序。它不是一张静态课表：先由模型为主题编排教学骨架，再补成可执行的章节内容；每节都可向 AI 讲师追问、完成开放题并得到逐题反馈。课程正文、参考答案和每次评测记录都保存在服务端数据库，浏览器只缓存当前课程 ID，因此清空缓存或换设备重新登录后仍能继续学习；但它还不是多端实时同步或正式学习档案。

```mermaid
flowchart LR
  User[用户] --> Home[今日首页 / 微信对话]
  Home --> Agent[/api/agent]
  Agent --> Parse[规则解析与会话状态]
  Agent --> Model[可选 OpenAI-compatible LLM]
  Home --> Quiz[/api/quiz]
  Quiz --> Model
  Home --> Course[/api/learning-program]
  Course --> Model
  WeChat[微信公众号] --> WechatAPI[/api/wechat]
  WechatAPI --> Agent
  Home --> Demo[/api/demo]
  Demo --> Seed[确定性 demo 数据]
```

## 快速开始

### 环境要求

- Node.js 22 或更高版本（Android WebView CDP smoke 需要 Node 22 的 `WebSocket` 全局对象）
- npm
- Windows PowerShell（Android 电脑模拟器脚本按 Windows 环境编写）
- Android 调试需要本机 Android SDK、AVD 和 Java 21；详见 [Android 构建说明](docs/ANDROID_BUILD.md)

### 本地 Web

```powershell
git clone https://github.com/redmaplewww/growth-loop-agent.git
Set-Location growth-loop-agent
npm.cmd install
Copy-Item -LiteralPath .env.example -Destination .env.local
npm.cmd run dev
```

打开 [http://127.0.0.1:3000/login](http://127.0.0.1:3000/login)，先创建本地账号。默认 `LLM_PROVIDER=demo`，不需要 API Key；新账号会初始化一个入门目标和三项入门行动。生产式本地回归可以使用：

```powershell
npm.cmd run build
npm.cmd run start -- --hostname 127.0.0.1 --port 3000
```

网页必须通过 Next.js 服务访问，不能直接双击 HTML 或部署到纯静态托管。第一次在另一台电脑配置服务，请先看 [网页服务配置手册](docs/WEB_SERVICE_SETUP.md)。

### 可选：启用成熟 RAG ingestion

默认不启动 Python 服务，资料仍走现有 TypeScript 回退，不影响网页运行。需要 LlamaIndex 分块时另开一个终端：

```powershell
Set-Location services\rag_ingestion
uv sync --extra dev --python D:\Python3.12\python.exe
.\.venv\Scripts\python.exe -m uvicorn growth_loop_rag.main:app --host 127.0.0.1 --port 8010
```

然后在根目录 `.env.local` 设置 `RAG_INGESTION_URL=http://127.0.0.1:8010` 并重启 Next.js。TXT/粘贴文本会直接采用 LlamaIndex；PDF/DOCX 还需要按 [ingestion 服务说明](services/rag_ingestion/README.md) 单独运行官方 MinerU API，并在 Python 服务进程中设置 `MINERU_API_URL`。完整 MinerU 模型不会随 `uv sync` 下载。

### 可选：启用本地 Embedding 与资料级语义检索

先确认 BGE 与 Qwen 模型位于 `.env.example` 所示目录，然后另开一个终端启动服务：

```powershell
npm run embedding:service
```

Next.js 使用 `.env.local` 中的 `EMBEDDING_SERVICE_URL=http://127.0.0.1:8020` 调用该服务；修改环境变量后必须重启 Next.js。当前尚未增加资料库 UI 按钮，可通过资料级 API 或下面的真实冒烟脚本生成并查询当前 active ChunkSet：

```powershell
npm run embedding:smoke -- --model bge-m3 --query "这份资料主要讲了什么？"
npm run embedding:smoke -- --model qwen3-embedding-0.6b --query "这份资料主要讲了什么？"
```

脚本默认选择最近更新且已就绪的资料；可额外传入 `--source <sourceId>`。第一次运行会生成向量，重复运行会按模型输入哈希复用已有向量。

### 可选：启用可恢复课程工作流

默认不启动 Python 编排服务，Next.js 会继续执行现有 TypeScript 流程。需要验证 LangGraph checkpoint/interrupt 时另开终端：

```powershell
Set-Location services\workflow
uv sync --extra dev --python D:\Python3.12\python.exe
Set-Location ..\..
npm run workflow:service
```

然后在 `.env.local` 设置 `WORKFLOW_SERVICE_URL=http://127.0.0.1:8030`，并为 Next.js 与 workflow service 配置相同的 `WORKFLOW_SERVICE_TOKEN` 后重启两者。阶段 D 的来源课程接线完成前，`WORKFLOW_REQUIRE_SOURCES=false` 保留非 grounded 课程兼容路径；设为 `true` 时资料不足会中断并等待用户补充。详细变量、Pydantic AI 可选配置和边界见 [workflow service 说明](services/workflow/README.md)。

### 检查项目

```powershell
npm.cmd run typecheck
npm.cmd run lint
npm.cmd run build
# 有受管 LLM 配置时，实际生成一门 Agent 课程并完成讲师追问、课后评分
npm.cmd run learning:smoke -- --require-llm
```

## 配置 LLM

服务端通过 OpenAI-compatible `/chat/completions` 调用模型。没有完整配置时，Agent 和测验会自动回退到本地规则，不会因为模型不可用阻塞页面或微信文本回复。

复制 `.env.example` 为 `.env.local` 后，按实际供应商填写以下变量：

```dotenv
LLM_PROVIDER=demo
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
```

也支持按 provider 读取对应变量，例如 `DEEPSEEK_BASE_URL`、`DEEPSEEK_API_KEY`、`DEEPSEEK_MODEL`，以及 `OPENAI_*`、`GLM_*`。密钥只放在本机环境变量、部署平台 Secret 或受管配置工具中，不能提交到 GitHub。

服务状态：

```powershell
Invoke-RestMethod http://127.0.0.1:3000/api/agent
```

返回值只包含模式、供应商和是否配置 endpoint/model 等非敏感状态，不返回 API Key。

## API 速查

### 账号与用户数据

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/auth/register` | 使用 username、password 和可选 displayName 创建账号 |
| `POST` | `/api/auth/login` | 使用 username 和 password 登录 |
| `POST` | `/api/auth/logout` | 删除当前数据库会话并清除 Cookie |
| `GET` | `/api/auth/me` | 获取当前登录用户的非敏感信息 |
| `GET` | `/api/dashboard` | 获取当前用户的目标、任务、记录、积分和统计 |
| `POST` | `/api/goals` | 创建当前用户的长期目标；接收 `title`、`description`、可选 `targetDate`（`YYYY-MM-DD`，须为将来日期）、`weeklyHours`（1–40）、`background` 和 `selfLevel`，同事务写入 `goals` 与 `goal_learning_profiles` |
| `POST` | `/api/tasks` | 创建当前用户任务；传 `lessonId` 时按课程章节建任务并写入 `task_lesson_links` |
| `PATCH` | `/api/tasks/[id]` | 切换当前用户所属任务的完成状态 |
| `POST` | `/api/activity-logs` | 调用 Agent 整理输入，并保存行动记录和奖励流水 |

SQLite 默认保存在 `data/growth-loop.sqlite`。可通过 `.env.local` 中的 `SQLITE_DATABASE_PATH` 修改位置。数据库文件、WAL 和 SHM 文件均已加入 Git 忽略。

### Agent 对话

```powershell
$body = @{
  message = '今天学习了 Agent 的工具调用，终于理解它和普通聊天的区别'
  conversationId = 'demo-user'
  context = '当前路线：学习 Agent 并开发自己的 Agent'
} | ConvertTo-Json

Invoke-RestMethod `
  -Method Post `
  -Uri http://127.0.0.1:3000/api/agent `
  -ContentType 'application/json' `
  -Body $body
```

`POST /api/agent` 接收 `message`（必填）、`conversationId`、`output`、`context`。返回 `reply`、`intent`、`extracted`、`mode`、`provider` 和 `replySource`。`GET /api/agent` 返回当前非敏感配置状态。

### 理解测验

生成题目：

```powershell
$body = @{
  action = 'generate'
  topic = 'Agent 工具调用'
  content = 'Agent 会先理解目标，再选择工具并根据结果继续行动。'
  output = '我理解了工具调用是让模型连接外部能力的桥梁。'
} | ConvertTo-Json
Invoke-RestMethod -Method Post -Uri http://127.0.0.1:3000/api/quiz -ContentType 'application/json' -Body $body
```

评分时提交 `action=grade`、原题目、学习底稿和 `answers`。如果模型已配置，返回 `gradedBy=llm`；否则返回 `gradedBy=rules`，并明确给出回看建议。

### AI 学习程序

课程状态：

```powershell
Invoke-RestMethod http://127.0.0.1:3000/api/learning-program
```

生成课程、AI 讲师追问和课后评分都通过同一条 API 的 `generate`、`tutor`、`grade` 动作完成，三者都需要登录：`generate` 只接收 `goalId`，`tutor` 和 `grade` 只接收 `programId + lessonId`，请求体中不出现课程正文。登录后可用 `GET /api/learning-program?program=current` 读回当前课程。默认的 Agent 示例会生成 5 节课程，每节含讲解、练习、交付物和 3 道开放题。完整调用契约、回退语义与实际 LLM 回归命令见 [AI 学习程序说明](docs/LEARNING_PROGRAM.md)。

### Demo 数据

```powershell
Invoke-RestMethod http://127.0.0.1:3000/api/demo
```

该接口继续提供确定性 seed，主要用于无账号 demo 和原型验收。登录后的工作台数据以 `/api/dashboard` 和 SQLite 为准，不再把 `/api/demo` 当作真实用户数据层。

## 微信公众号接入

当前实现的是“服务端回调 + 明文文本模式”，不是小程序登录或微信支付。配置步骤：

1. 准备公网 HTTPS 域名和可访问的 Next.js 部署。
2. 在部署 Secret 中配置 `WECHAT_APP_ID`、`WECHAT_APP_SECRET`、`WECHAT_TOKEN`。
3. 微信公众平台服务器地址填写 `https://你的域名/api/wechat`。
4. 选择明文模式，完成首次 GET 签名验证。
5. 用文本消息 POST 回归；模型超时会自动使用本地回退话术。

详细签名规则、XML 字段、状态接口和当前边界见 [微信公众号接入说明](docs/WECHAT_INTEGRATION.md)。当前 MVP 未启用安全模式/兼容模式的密文解密，正式上线前必须补齐加密回调、重放保护、限流和生产监控。

## Android APK

当前仓库已包含 Android 工程和 debug APK：

- 工程：[android/](android/)
- APK：[artifacts/android/growth-loop-debug.apk](artifacts/android/growth-loop-debug.apk)
- 电脑浏览器：`http://127.0.0.1:3000/`
- Android Emulator：`http://10.0.2.2:3000/`

启动本地服务后，在项目根目录执行：

```powershell
npm.cmd run android:debug   # build + sync + Gradle + install + launch
npm.cmd run android:run     # 使用现有 APK 安装并启动
npm.cmd run android:status
npm.cmd run android:stop
```

面向 AI 的完整调试顺序：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/debug-apk.ps1 -Action doctor
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/debug-apk.ps1 -Action run -Build
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/debug-apk.ps1 -Action smoke
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/debug-apk.ps1 -Action logs
```

详见 [Android APK 调试手册](docs/ANDROID_APK_DEBUG_AI.md) 和 [Android 移动端产品设计](docs/ANDROID_MOBILE_PRODUCT_DESIGN.md)。APK 是 debug 签名，并默认从电脑模拟器读取远程 Next.js 服务；正式发布还需要 HTTPS、release keystore、AAB、真机回归和隐私合规。

## 文档地图

| 文档 | 内容 |
|---|---|
| [项目亮点与量化记录](项目亮点.MD) | 当前可证明成果、后续差异化能力、量化指标与简历表述账本 |
| [开发者与 AI 手册](docs/DEVELOPER_HANDBOOK.md) | 从 clone、配置、开发、测试到发布的完整交接手册 |
| [网页服务配置](docs/WEB_SERVICE_SETUP.md) | 从干净 clone、环境变量到 Web/Node 服务验收；解释静态托管、API 和 Android 地址边界 |
| [公网 Demo 部署](docs/DEMO_DEPLOYMENT.md) | Zeabur GitHub 部署、SQLite 持久化、LLM Secret、验收步骤与邀请测试边界 |
| [AI 学习程序](docs/LEARNING_PROGRAM.md) | 通用课程编排、AI 讲师、课后评分、API 契约与 LLM smoke |
| [V1 实施方案](docs/IMPLEMENTATION_PLAN_V1.md) | 教学质量优先总路线、可信来源、框架迁移与正式考核的版本边界 |
| [V0.4.2 实施方案](docs/IMPLEMENTATION_PLAN_V042.md) | 诊断证据驱动的最小学习闭环：能力拆解、初始诊断、按需生成课程、盲评与掌握度回边 |
| [V0.4.3 实施方案](docs/IMPLEMENTATION_PLAN_V043.md) | 结构化课程内容引擎、教学质量门禁、题目追溯，以及基础课件阅读器与待完成交互边界 |
| [V0.4.4 实施方案](docs/IMPLEMENTATION_PLAN_V044.md) | 私有资料导入、Schema V8-V10、结构化父子切片、文本向量检索、可恢复课程编排、块级/题目级来源快照与 RAG 验收标准 |
| [资料驱动教学闭环子方案](docs/IMPLEMENTATION_PLAN_V044_GROUNDED_TEACHING.md) | 目标资料绑定、本地 Agentic RAG Tool、LangGraph/Pydantic AI 最小主图、证据课程、来源题目、固定评分和联网资料补充的直接实施顺序 |
| [RAG 总体架构](docs/RAG.MD) | 上传、链接、联网搜索、结构解析、父子切片、不可变 ChunkSet、检索快照与教学证据的统一关系 |
| [Agent 角色与信息边界](docs/AGENT_ROLES.md) | 规划员/导师/考官/复盘员的职责、能看到什么、输出契约与复用矩阵 |
| [产品设计方案](docs/PRODUCT_DESIGN_V1.md) | 产品目标、用户闭环、Agent、游戏化和微信路线 |
| [微信公众号接入](docs/WECHAT_INTEGRATION.md) | 微信服务器配置、签名校验和文本回调 |
| [Android 构建](docs/ANDROID_BUILD.md) | SDK、AVD、Capacitor 和 Android Studio |
| [Android APK 调试](docs/ANDROID_APK_DEBUG_AI.md) | doctor/build/install/run/smoke/logs 调试链路 |
| [移动端产品设计](docs/ANDROID_MOBILE_PRODUCT_DESIGN.md) | 一屏 AI 首页、底部导航和移动交互约束 |
| [.project-to-act](.project-to-act/PROJECT_OVERVIEW.md) | 项目目标、范围、进度、版本和验收证据 |

## 数据、安全与发布边界

- 已有 SQLite 数据库和基于 Session 的本地多用户隔离；当前仍没有后台定时任务、数据导出、忘记密码或管理员系统。
- 用户资料原文件 BLOB、抽取文本和索引元数据保存在同一个 SQLite 数据库；公开 API 不接收客户端 `userId` 或文件路径。当前尚未实现资料导出、恢复和配额管理，部署时必须使用持久化卷并备份数据库。
- 密码字段保存在 `users.password_hash`，使用 scrypt 加盐处理；任何代码和文档都不得记录用户明文密码。
- `action_streak_days` 表示连续产生有效行动记录的天数，不是单纯点击按钮的签到天数。
- `goals.progress_percent` 已持久化；LLM 自动调整进度仍需增加服务端范围、证据和变化幅度校验。
- LLM 只负责理解、建议、出题和评分；任务、XP、积分等写操作应继续由规则和受控服务端处理。
- 不要提交 `.env.local`、API Key、微信 Token、`android/local.properties`、Android build 目录、模拟器镜像或本机 SDK 路径。
- 微信回调当前只支持明文文本；不要在未补齐加密、重放保护、限流和审计前接收生产敏感消息。
- 仓库目前没有单独的开源许可证文件；如需对外允许复用，请先补充许可证和第三方依赖声明。

## 常见问题

| 现象 | 处理 |
|---|---|
| 电脑能打开，APK 打不开 | 确认 Next 服务监听 `127.0.0.1:3000`，APK 使用的是 `10.0.2.2:3000`；先跑 `doctor` |
| 页面显示旧版本 | 重启 `next start` 或重新执行 `npm.cmd run dev`，再重新打开 APK；当前 APK 默认加载远程页面 |
| `smoke` 找不到页面 | 确认应用已启动、ADB 设备为 `device`，然后重新执行 `run` 再 `smoke` |
| LLM 没有返回 | 查看 `/api/agent` 状态和环境变量；没有完整配置时预期会走 demo/rules 回退 |
| 微信首次验证失败 | 检查公网 HTTPS、`WECHAT_TOKEN`、服务器 URL 和服务器时间；不要把 token 写进源码 |
| Android ADB `offline` | `npm.cmd run android:stop` 后重新 `npm.cmd run android:debug`，必要时重启 adb server |

## 贡献与提交

1. 从 `main` 创建短分支，修改前先阅读 `AGENTS.md` 和相关 `.project-to-act` 文档。
2. 只提交本次任务范围内的文件，避免把密钥、构建产物和模拟器缓存带入提交。
3. 至少运行 `typecheck`、`lint`、`build`；涉及 Android 时再运行 `android:debug`、`doctor`、`smoke`、`logs`。
4. 每次功能、接口、数据库结构或运行方式发生变化，都要在 README 的“变更记录”中标注 `新增`、`修改`、`修复` 或 `暂未实现`。
5. 提交信息说明意图，PR 描述列出变更、验证命令和已知边界。

## 当前版本

`0.2.0-prototype` · Android 移动壳 v4 · 已加入本地 SQLite、username 账号、数据库 Session 与首批用户数据持久化。生产级数据库、后台调度、微信加密模式、release 签名和真实用户验收属于后续版本范围。
