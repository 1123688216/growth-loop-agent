# Growth Loop 项目亮点与量化记录

> 本文是一份持续更新的项目亮点账本。它只记录已经有代码、测试或实验支撑的结论；计划中的能力单独标注，不能提前写成已完成成果。

## 1. 项目定位

Growth Loop 不是一个通用聊天机器人，而是一个面向个人学习与成长的证据驱动型 Agent：

```text
创建目标 → 判断用户缺什么 → 安排课程和每日任务
→ 学习与答题 → 用考核证据确认完成 → 更新能力画像
→ 调整后续课程和任务
```

项目最终要解决的核心问题不是“如何让 AI 多说一些内容”，而是：

- 如何判断用户真正缺少什么能力；
- 如何把长期目标拆成能执行、能验证的学习任务；
- 如何避免用户只浏览内容，却没有真正掌握；
- 如何根据答题和行动证据持续调整后续计划；
- 如何让 Agent 的决策、成本和效果可以回放、评测和解释。

## 2. 当前已经可以说明的亮点

### 2.1 服务端成为课程唯一数据源

课程、章节、题目和评分记录已经从浏览器本地快照迁移到 SQLite。浏览器只保存当前 `programId`，课程正文由服务端按用户身份读取。

这次改造带来的直接收益：

- 清空浏览器缓存后，课程正文和章节进度不会随之丢失；
- 换设备重新登录后，可以从数据库恢复当前课程；
- `tutor` 和 `grade` 请求不再回传整份课程；
- 移除了每次最多 180KB 的课程 JSON 重复传输；
- 服务端能够校验课程、章节和当前用户之间的归属关系。

可用于简历的表述：

> 将课程链路从客户端快照重构为服务端唯一数据源，以 `programId + lessonId` 替代每次最大 180KB 的课程正文回传，实现课程跨会话恢复和用户级数据隔离。

### 2.2 评分标准与客户端数据隔离

项目区分了两套课程契约：

- `CourseQuestion`：客户端可见，只包含题干和提示；
- `AuthoredCourseQuestion`：仅服务端持有，包含 `referenceAnswer` 和 `rubric`。

服务端在返回课程前主动剥离参考答案和评分标准。用户提交答案时，只提交答案和课程标识，评分过程读取数据库中的固定题目快照。

可用于简历的表述：

> 设计公开题目与服务端评分题目的分层类型契约，使客户端课程响应中的参考答案与 rubric 字段为 0，避免用户篡改评分标准或提前读取答案。

### 2.3 学习记录从“结果覆盖”变成“证据历史”

每次课后评测都会新增一条 `lesson_assessment_attempts`，`attempt_number` 按用户和章节递增，重测不会覆盖首次结果。

这使系统能够进一步计算：

- 首次通过率；
- 平均通过尝试次数；
- 首次与重测的分数提升；
- 哪些知识点经常需要补充讲解；
- 某次任务完成是否真的有合格答题记录。

可用于简历的表述：

> 将课后评分设计为追加式证据记录，保留每次答案、评分、反馈和尝试序号，为学习增益、补救效果和能力掌握度计算提供可追溯数据。

### 2.4 用关系数据替代标题匹配

课程任务通过 `task_lesson_links` 与章节建立正式关系，任务标题、时长和合格线由服务端课程章节提供，不再依赖标题相同或特殊 ID 前缀进行匹配。

可用于简历的表述：

> 使用任务—章节关系表替换脆弱的标题匹配逻辑，使每日任务、课程跳转和评测结果具备稳定、可查询的关联。

### 2.5 事务化的数据模型

当前数据库 Schema V2 共 20 张表，覆盖：

- 用户、会话、目标和任务；
- 目标自评和能力拆分；
- 课程、模块和章节；
- 诊断题、诊断尝试和课后考核；
- 工作流运行和 Agent 调用统计；
- XP、Coin 与成长记录。

目标和学习档案在同一事务写入；课程、模块和章节也在同一事务写入，避免部分成功后留下不完整数据。

当前验证结果：

```text
schemaVersion: 2
tableCount: 20
integrity: ok
TypeScript: passed
ESLint: passed
Next.js production build: passed
```

## 3. 项目完成后最重要的差异化亮点

项目真正的差异化不应是“使用了 LangGraph、Pydantic AI 或多个 Agent”，而应是：

> 建立一个以诊断证据驱动的自适应学习闭环：系统先判断用户缺什么，再安排课程；课程任务只有在考核合格后才能完成，并根据持续产生的学习证据更新能力画像和后续计划。

对应能力包括：

### 3.1 基于诊断的个性化路线

- 创建目标时选择初学者、一知半解或小有所成；
- 初学者直接进入基础课程；
- 另外两档先进行能力诊断；
- 诊断结果写入 `skill_mastery`；
- 每节课程明确对应一个待提升能力；
- 已掌握内容减少重复教学，薄弱能力获得更多练习。

### 3.2 有证据约束的任务完成

- 普通任务可以手动完成；
- 课程任务必须存在达到合格线的评测记录；
- 章节合格和关联任务完成应在同一服务端事务内完成；
- 任务完成状态可以追溯到具体试卷、答案和得分。

### 3.3 可暂停和恢复的工作流

LangGraph 负责目标创建、诊断等待、评分、课程生成和补救学习等长流程：

```text
保存目标 → 生成能力模型 → 条件分支
→ 生成诊断 → 等待用户答题 → 评分
→ 更新掌握度 → 生成课程 → 保存任务
```

Pydantic AI 只负责需要模型判断的节点，并输出经过 Pydantic 校验的结构化对象；数据库事务、权限、阈值和任务状态仍由普通程序控制。

### 3.4 可观测、可评测的 Agent 系统

每次 Agent 调用最终需要记录：

- Agent 角色和工作流节点；
- 模型、Provider 和 Prompt 版本；
- 输入、结构化输出和回退原因；
- Prompt Token、Completion Token 和总 Token；
- 延迟、错误类型和重试次数；
- 关联用户、目标、章节和工作流。

这样才能回答“哪种流程更有效”“哪个 Agent 最耗 Token”“加入诊断后是否真的提高效果”。

## 4. 当前还不能对外宣称的能力

以下内容目前只有 Schema 或设计文档，尚不能写成已完成成果：

- 三档自评 UI；
- LLM 诊断题生成和固定题目快照；
- `goal_skills` 和 `skill_mastery` 的真实读写；
- 根据能力薄弱点生成课程；
- LangGraph 持久化工作流；
- Pydantic AI 结构化 Agent 节点；
- 工作流暂停、恢复和故障回放；
- `agent_runs` Token 与延迟的真实写入；
- 根据学习证据动态调整后续任务；
- 学习效果相对固定课程提高了多少。

## 5. 当前需要优先解决的问题

### P0：诊断分支不能被课程生成绕过

当前目标创建成功后会立即生成课程。加入自评功能时必须改为：

```text
beginner → 生成基础课程
familiar/intermediate → 生成诊断并等待答题 → 评分 → 生成课程
```

验收标准：非初学者在诊断完成前，数据库中不能出现正式激活的课程。

### P0：章节合格与任务完成必须原子化

目前章节合格与关联任务完成是两次独立请求，存在状态不一致风险。

验收标准：

- 评分合格后，同一事务更新章节和关联任务；
- 没有合格评测记录时，服务端拒绝完成课程任务；
- 所有已完成课程任务都能查询到对应的合格评测证据。

### P0：观测表必须接入真实调用

虽然已经存在 `agent_runs`，但当前 LLM 调用尚未采集 Token、延迟和错误。

验收标准：每次课程生成、讲解和评分调用都产生一条成功、失败或 fallback 记录，并且满足：

```text
total_tokens = prompt_tokens + completion_tokens
latency_ms >= 0
status in completed / failed / fallback
```

### P1：清理文档中的新旧状态冲突

README 和实施方案中仍保留部分已经过时的“课程仅存 localStorage”“创建目标 API 尚未修改”等描述。历史记录可以保留，但必须标注已被哪个版本替代。

## 6. 可量化指标设计

### 6.1 当前已经能够安全使用的数字

| 指标 | 当前结论 | 证据类型 |
|---|---|---|
| 课程请求体 | 消除每次最多 180KB 的课程快照回传 | 旧接口上限与新接口契约 |
| 答案保护 | 客户端课程题目中 `referenceAnswer` / `rubric` 字段为 0 | 公开类型和响应转换 |
| 数据结构 | Schema V2 共 20 张表 | 数据库校验脚本 |
| 数据完整性 | SQLite `integrity_check = ok` | 自动校验输出 |
| 工程构建 | TypeScript、ESLint、生产构建通过 | 本地验证输出 |
| 尝试记录 | 重测新增记录，不覆盖历史尝试 | 唯一约束与 smoke 断言 |

### 6.2 完成评测体系后可以计算的指标

| 指标 | 计算方式 | 说明 |
|---|---|---|
| 有效完成率 | 有合格答题记录的已完成课程任务 / 全部已完成课程任务 | 目标应为 100% |
| 学习提升 | 后测分数 − 前测分数 | 反映绝对分数变化 |
| 归一化学习增益 | `(后测 − 前测) / (100 − 前测)` | 减少初始水平差异影响 |
| 规划命中率 | 与诊断薄弱能力直接关联的课程数 / 总课程数 | 验证路线是否真正个性化 |
| 首次通过率 | 首次答题合格章节数 / 已答题章节数 | 判断课程难度与讲解质量 |
| 补救有效性 | 重测分数 − 首次分数的平均值 | 判断反馈和补充讲解是否有效 |
| 结构化输出成功率 | 无需修复或规则回退的有效输出 / 总模型调用数 | 验证 Pydantic AI 契约稳定性 |
| 工作流恢复率 | 故障注入后成功恢复的工作流 / 故障工作流 | 验证 LangGraph 持久化价值 |
| 模型回退率 | fallback 调用数 / 总调用数 | 反映模型和解析稳定性 |
| 成本效率 | 总 Token 或费用 / 合格章节数 | 避免只优化成功率不考虑成本 |
| 接口性能 | P50、P95 延迟 | 分别统计生成、讲解和评分 |

## 7. 对照实验建议

为了得到可以写在简历中的真实百分比，至少保留三组运行模式：

| 模式 | 能力 |
|---|---|
| Baseline A | 不诊断，按目标生成固定课程 |
| Baseline B | 只有用户自评，不进行测试 |
| Experiment C | 自评 + 诊断 + 能力画像 + 自适应课程 |

使用相同目标、相同模型、相同后测题和相近随机条件进行对照，建议覆盖 30–50 组固定学习者画像，并对关键模型流程重复运行，避免把一次模型波动写成系统提升。

最终可以形成这样的结论：

> 在 N 组固定学习者画像上，相比无诊断固定课程基线，自适应路线使后测成绩提高 X%，薄弱能力课程命中率达到 Y%；结构化输出成功率达到 Z%，工作流故障恢复率达到 W%。

其中 `N/X/Y/Z/W` 必须来自保存下来的评测结果，不能根据主观体验填写。

## 8. 简历表述草稿

### 当前版本可以使用

> 设计并实现面向个人成长的学习闭环 Agent，打通目标创建、AI 课程编排、章节学习、开放题评分、任务关联与学习证据持久化。

> 将课程状态从浏览器快照迁移为服务端唯一数据源，以 ID 引用替代每次最大 180KB 的课程回传，并通过用户归属校验实现跨会话恢复和数据隔离。

> 分离客户端题目与服务端评分契约，使参考答案和 rubric 不下发到浏览器；采用追加式答题记录保留每次答案、评分和反馈，为后续能力画像与学习效果评测提供数据基础。

> 设计 20 表 SQLite 学习闭环模型，覆盖目标、自评、能力、课程、诊断、课后评测、工作流和 Agent 调用观测，并通过事务、外键、约束和完整性脚本保证数据一致性。

### LangGraph 和评测完成后再使用

> 基于 LangGraph 构建可中断、可恢复的目标诊断工作流，在规划、出题、评分和教学节点中使用 Pydantic AI 约束结构化输出，并由确定性服务负责权限、事务和进度计算。

> 建立面向学习 Agent 的离线评测体系，对固定课程、自评课程和诊断自适应课程进行对照，围绕学习增益、有效完成率、恢复率、Token 成本和 P95 延迟量化系统效果。

## 9. 亮点更新规则

后续每完成一个版本，都在本文补充：

1. 完成了什么能力；
2. 对应代码、测试或数据证据；
3. 相比之前版本改变了什么；
4. 新增了哪些可计算指标；
5. 哪些简历描述从“计划中”升级为“可以使用”；
6. 是否存在回归、限制或尚未解决的问题。

禁止只记录“接入了某个框架”。每个框架能力都必须对应一个可以验收的产品或工程结果。
